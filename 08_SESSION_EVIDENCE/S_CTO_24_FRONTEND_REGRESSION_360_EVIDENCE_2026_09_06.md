# SESSION ID S-CTO-24 — FRONTEND REGRESSION 360 (READ-ONLY) — EVIDENCE

**Status:** FINISHED — COMPLETE (read-only diagnosis; all 11 root-caused with fix plan + build-session grouping).
**Date:** 2026-09-06 · **Mode:** MODE A discovery/forensics · **Change:** NONE (no code, commit, deploy, prod mutation).
**Skills:** tawadoo-source-truth (activated), tawadoo-runtime-provider-qa (activated).

## FIVE-LINE OPENER (corrected after preflight)
- SESSION: S-CTO-24 — FRONTEND REGRESSION 360 (READ-ONLY, 11 items)
- BRANCH: web `tawadoo_web_js` = Ramzi_V2 @ **8b0a815f** (staging) · prod ref = **origin/main @ a6499893** · api `tawadoo_api_js` = Ramzi_V2 @ **8d55a20**
- MISSION: root-cause FR-1..FR-11 from source, staging-vs-prod-main, fix plan each. NO change. Never break mobile.
- ORDER: business-truth → source trace → per-item component + diff + regressing commit → root cause → fix plan → grouping → evidence
- TREE STATE: web dirty = unrelated evidence/playwright artifacts (pre-existing, untouched). Touched ONLY this evidence file. HEADs unchanged (verified end).

## PROVENANCE / DRIFT NOTE (read-only, not integrating)
- `origin/main` (a6499893) is **NOT an ancestor** of Ramzi_V2 (merge-base 8b0d7440; Ramzi_V2 +38 / origin/main +1335). This is the known legacy-prod-vs-future-line drift (§28). Prod-main is used here ONLY as the founder-liked visual reference for diffing — no integration, no touch.
- Almost every "regression" below is an **ADDITION new in Ramzi_V2** (display-ads platform, LiveAuctionStrip, card rewrite, chat rebuild, client-side search re-fetch). These are not reverts of prod — prod simply never had them. Fix plans therefore ADJUST the new Ramzi_V2 code toward the prod-liked appearance, they do not "restore a deleted file."

## BUSINESS TRUTH ANCHORS USED
- Moderation = human gate, sacred (do not touch). Distribution = silent (never surface to user). FCM + email channels exist (relevant to FR-2). Lead-gen: unlock = included-allowance-first else 10 coins; mark-contacted requires unlock-first. Copy changes are §30 (founder-approved FR/AR/EN before any build).

---

## FR-1 — PINK RECTANGLE (auction promo block on the grid)
- **Component / file:line:** `tawadoo_web_js/src/components/home/LiveAuctionStrip.tsx:62` — `<section className="w-full bg-gradient-to-r from-orange-400 via-rose-400 to-purple-500 py-3 px-4">`. Title `{auction.title}` (~:70, e.g. truncated "Auctiontisxsatr…"); CTA `{t('live_auction.cta')}` = "زايد الآن" / "Bid now" (`public/locales/ar.json:4300`, `en.json:4300`, ~:91).
- **Where it renders:** homepage ONLY — `src/sections/home/view/home-view.tsx:289` and `src/sections/home/view/HomeBelowFold.tsx:237`. It is NOT wired into the search/listing grid. The "pink rectangle on the listing grid" the founder saw is this full-bleed homepage strip sitting above the product feed.
- **Staging-vs-prod-main diff + regressing commit:** ADDITION new in Ramzi_V2 — `git cat-file -e origin/main:src/components/home/LiveAuctionStrip.tsx` → absent on main. Introduced by **97dbaa1b** "feat(live-auction-strip): soonest-ending auction highlight"; spacing tweak **44c4cab8**.
- **Root cause:** A full-width `<section>` with a hard-coded orange→rose→purple gradient and no card/container chrome, no image, rendering an untranslated/garbled/placeholder auction title. It reads as a raw pink block because (a) the gradient is unconditional, (b) it has no rounded card/padding/shadow container to look intentional, (c) the title is a raw truncated string. It returns null only when `loading || !auction` (:51), so a bad-title auction still paints the bar.
- **Fix plan (WEB-only):** Restyle the strip into a proper contained card (rounded, bordered, constrained max-width, an auction thumbnail, softened palette) OR gate it to only render when there is a genuinely live/soon auction with a clean title. Since it is absent on prod, safest quick win = wrap in a max-width container + card chrome + guard on title quality; founder decides whether the strip stays at all.
- **Mobile safety:** Component is `w-full` at all breakpoints; the countdown center block is `hidden sm:flex`. A container/card restyle applies equally to mobile and does not rely on `lg:` desktop-only logic — safe. No shared grid component touched.
- **Layer:** WEB-only. **RED/YELLOW:** YELLOW (cosmetic, no data loss). **Quick-win.**

## FR-2 — AUCTION CARD RENDER + NOT-STARTED SUBSCRIBE/NOTIFY POLICY
- **Component / file:line:** Auctions render through `ProductCard` when `product.type === 'bid'` — there is NO separate auction-card component in the grid. `tawadoo_web_js/src/components/product/productCard.tsx`: bidState machine (NOTSTART/START/END) computed ~:214-233; countdown chip ~:440-500 `absolute -bottom-4 ... bg-gradient-to-r from-sand-100 via-sky-100 to-cyan-100` with `hidden lg:block` countdown text. Detail page pre-start state: `src/sections/auction/view/auction-view.tsx:266-283` sets `bidState="NOTSTART"` + shows CountDown, fires `auction_countdown_viewed`.
- **Bad render root cause:** the countdown chip is `absolute -bottom-4` overlapping the card edge and the countdown label is desktop-only (`hidden lg:block`), so a NOT-started auction shows an awkward/empty overlapping chip; combined with the FR-6 gradient-badge defect the card looks broken. Same card-rewrite churn as FR-6.
- **SUBSCRIBE/NOTIFY PATH — DOES IT EXIST? → PARTIALLY (channels only, NO auction-start hook).**
  - WEB: no "notify me / remind me" CTA anywhere (`auction-view.tsx` NOTSTART branch has only a countdown; grep `remind_me/notifyOnStart/reminder` = 0 matches).
  - API: no subscribe/watch endpoint (`bidRoom.controller.ts` = create/list/get only); no auction-start cron/trigger; no `auction_start` notification code.
  - `auction_reminder_set` / `auction_watched` are ANALYTICS event names only (allowlist), not a feature; `auction_reminder_set` is never emitted.
  - Delivery channels DO exist: FCM push (`PushTokenService`) + email (`EmailService`) + in-app via `NotificationService.createWithOptions({notification,email})`.
- **Fix plan (CROSS-LAYER + POLICY):** Per founder rule, a not-yet-started auction must NOT display unless the user can subscribe to be notified. Building this = (1) API subscribe endpoint + entity (auction-start subscription), (2) WEB pre-start CTA in the NOTSTART branch (card + detail), (3) a start-time trigger (cron/scheduler) that fans out via the existing in-app + FCM + email channels. Card visual fix rides with FR-6.
- **Mobile safety:** subscribe CTA is additive; card countdown-label logic is already `lg:`-gated — restyle the chip position without changing base (mobile) classes.
- **Layer:** CROSS-LAYER (web + api). **RED/YELLOW:** YELLOW visual + policy gap. **Needs-care** (new endpoint/trigger; founder policy decision on hide-vs-subscribe).

## FR-3 — PAGINATION (page 1→2 dead across listings)
- **Components / file:line:** `src/components/pagination/pagination.tsx` (dumb, correct — fires `onPageChange`). Consumer `src/sections/searchResults/view/search-results-view.tsx`: `handlePageChange` :227-236 does `newParams.set('pg', page)` + `router.push('/${locale}/search?...')` (client-side nav, no remount); Pagination rendered when `totalPages >= 2` (~:970). Fetch owner `src/components/filter/filter.tsx`: reads `pg` ONCE in `initSearchParams()` (~:222-230, runs once on mount via `useEffect([],...)`); sends `params.page = currentPage; params.limit = 24; fetcher('publications/search', params)` (~:451-464). The re-fetch effect (~:360-377) depends only on filter/sort state and explicitly `setCurrentPage(1)` on every fire — it does NOT depend on `currentPage`/`searchParams`.
- **API confirmed correct:** `tawadoo_api_js` `publication.controller.ts` `@Get('search')` parses `page`/`limit`, returns `{totalItems,totalPages,currentPage,pageSize,publications}`; service computes `from=(page-1)*limit`. **Param name matches** (web sends `page`, API expects `page`). No API mismatch.
- **Staging-vs-prod-main diff + regressing commit:** **0ffb088f** "fix(search): unify sort/filter params and client-side re-fetch architecture" added the client-side re-fetch effect but scoped its deps to filter/sort only and hard-resets to page 1, with no `pg`/`currentPage` branch. Precondition **2e9fd7d7** "fix(search): client-side navigation …" moved search to `router.push` — a full navigation would previously have remounted `Filter` and re-read `pg`; after client-side nav there is no remount, so the missing dependency became fatal.
- **Root cause:** clicking a page updates the URL `?pg=2` via client-side `router.push` (no remount) → `initSearchParams()` (only reader of `pg`) does not re-run → no effect watches `currentPage`/`searchParams` → `getAnnouncements()` never re-fires with `page=2`. Grid stays on page 1. Pagination is architecturally unwired for page changes.
- **Fix plan (WEB-only):** make the fetch react to page changes — add `currentPage` (or the `pg` searchParam) to a fetch-trigger effect in `filter.tsx`, and stop the unconditional `setCurrentPage(1)` from clobbering a page navigation (only reset to 1 when a real filter/sort changes, not on a pure page change). No API change.
- **Mobile safety:** logic-only (data-fetch trigger), no layout/breakpoints — mobile unaffected.
- **Layer:** WEB-only (API verified fine). **RED/YELLOW:** RED (core functional break, all listings). **Needs-care** (touches the shared search/filter re-fetch architecture — one bounded slice, verify no double-fetch/reset regressions).

## FR-4 — CHAT / MESSAGES DON'T WORK
- **Components / file:line:** Active desktop path is the rebuilt tree under `SocketProvider`: `src/sections/dashboard/message/view/message-view.tsx` wraps `<SocketProvider><InboxView/></SocketProvider>`. `src/providers/SocketProvider.tsx` inits `io(process.env.NEXT_PUBLIC_API_BASE_URL!, { query:{token,entityId}, transports:['websocket'], ... })` (websocket-only, deliberate — comment cites staging ALB `tw-staging-tg-api` stickiness disabled → polling gets 400). `src/components/chat/ChatView.tsx`: SEND = REST `post('messages/${threadId}',...)`; HISTORY = REST `fetcher('messages/by-thread/${threadId}')`; RECEIVE/typing/read = socket `socket.on('newMessage'|'messagesRead'|'typing')` (~:50-71), room join via `joinThread` emit.
- **API confirmed healthy:** `tawadoo_api_js/src/modules/realTime/real-time.gateway.ts` — `@WebSocketGateway` CORS allowlist INCLUDES `https://staging.tawadoo.ma` (:25-31, credentials:true); JWT handshake; `@SubscribeMessage('joinThread'|'sendMessage'|'typing')`; `notifyMessage` emits `newMessage` to `messageThread_<id>`. Message REST controller endpoints present. NOT an API/CORS/namespace outage.
- **Env cleared as cause:** `.github/workflows/deploy.yml` copies `.env.staging` (`NEXT_PUBLIC_API_BASE_URL="https://api-staging.tawadoo.ma/"`) before build; Dockerfile `COPY . .` then build, no `.dockerignore`. The stale `localhost` in committed `.next/required-server-files.json` is not what CI ships. Socket URL is correct on staging.
- **Staging-vs-prod-main diff + regressing commit:** full chat rebuild **144ec858** "feat(chat): Phase 2B frontend — full chat rebuild with shared socket…" (+957/-763), followed by a chain of connect/auth-timing fixes (**9bd71355, 16547e12, 788c0b10, 8fa8515e, 638e0278**) — evidence this surface is fragile and still churning.
- **Root cause (source-level; needs runtime confirm):** because SEND + HISTORY are REST and independent of the socket, "messages don't work" splits: (a) if threads/history/send fail → REST/auth/UI in the rebuilt InboxView/ChatView; (b) if send works but nothing arrives live / "unable to load" → the shared `SocketProvider` never reaches `connected` (websocket upgrade/handshake against `api-staging.tawadoo.ma`, or `isTokenUsable`/entity bail-out), leaving `socket` null so listeners never attach. The provider's own comments document exactly this prior class of bug.
- **CROSS-LAYER FLAG / infra dependency:** the client is websocket-only by design → requires the staging **ALB to permit WebSocket upgrade** (LB config, not app code). This must be confirmed live before the build session.
- **Fix plan:** FIRST reproduce on staging in a real browser (RED-flag runtime QA): (1) do `message-threads/my-threads` + `messages/by-thread` + `POST messages/:id` succeed? (2) does the socket reach `connected` (WS upgrade OK, token usable, joinThread emitted)? Then fix WEB (socket connect/auth-timing, or REST/auth) accordingly. API app needs no change unless the ALB WS upgrade itself fails.
- **Mobile safety:** logic; no desktop-only layout involved.
- **Layer:** WEB primary; infra (ALB WS) to verify. **RED/YELLOW:** RED (core functional). **Needs-care** (must runtime-reproduce first; do not blind-patch the rebuilt socket).

## FR-5 — DESKTOP DETAIL LAYOUT (empty space + horizontal "Grow your business" strip)
- **Component / file:line:** Regression is in the PARENT container `src/sections/productDetails/view/product-details-view.tsx`, NOT `productDetails.tsx` (its gallery|info grid at `productDetails.tsx:885 grid-cols-1 lg:grid-cols-2` is byte-identical to prod). Parent: outer row `:137` `flex flex-col lg:flex-row justify-center p-4 ... lg:space-x-4`; content column `:139` `flex-1 max-w-full mx-auto`; NEW desktop-only right ad rail `~:192` `hidden lg:block w-[300px] shrink-0` → `sticky top-24` → `<DisplayAdBanner slot="listing_sidebar" />`; mobile-only ad `~:152` `lg:hidden` `slot="listing_below"`.
- **The "Grow your business" strip:** `src/components/ads/AdFallbackPlaceholder.tsx` — the horizontal "Advertise on Tawadoo" fallback card (Megaphone + title + CTA to `/advertising`), rendered by `DisplayAdBanner.tsx` no-creative branch. `src/utils/ads/slot-config.ts` sets `listing_sidebar` = variant `'compact'` (horizontal), so the 300px rail shows a horizontal strip.
- **Staging-vs-prod-main diff + regressing commit:** **523adc9a** "feat: implement display ads platform" (present on Ramzi_V2, NOT ancestor of origin/main). It added the desktop right ad rail + the AdFallbackPlaceholder. Prod a6499893 had NO ad banners on the detail page — its layout was just the content column with wide margins. So the founder's "prod = vertical dual side banners" is the OLD centered-content-with-margins look; there is NO dual-banner component to restore.
- **Root cause:** `lg:flex-row justify-center` + a `flex-1 max-w-full mx-auto` content column that does not stretch, plus a fixed `w-[300px]` rail on one side → on desktop the inner (max-width-capped) content collapses toward center leaving large flanking whitespace, and the single asymmetric 300px rail shows the horizontal fallback strip. Desktop-only; below `lg` the row is `flex-col` and the rail is `hidden`.
- **Fix plan (WEB-only, desktop-scoped):** in `product-details-view.tsx`, make the content column stretch and give a balanced desktop layout — e.g. remove `justify-center`/`mx-auto` on the `flex-1` column so it fills, and/or set an explicit `lg:` width split so the row fills the viewport; and change the sidebar slot to a VERTICAL banner variant (edit `slot-config.ts` `listing_sidebar` variant, or render a vertical rail) if the founder wants the side-banner look, or drop the rail toward the clean prod look. Do NOT touch `productDetails.tsx:885` grid.
- **Mobile safety:** every differing class is `lg:`/`hidden lg:block`/`lg:hidden`; no `useMediaQuery`. Desktop-only edits are CSS-safe for mobile.
- **Layer:** WEB-only. **RED/YELLOW:** YELLOW (cosmetic/layout). **Needs-care** (touches the shared ad-slot config used elsewhere — verify other slots; one bounded slice).

## FR-6 — PRODUCT CARDS (placeholder-heavy / ugly vs clean prod cards + tags + side banners)
- **Component / file:line:** `src/components/product/productCard.tsx` — substantially REWRITTEN vs prod (411→556 lines; `git diff origin/main..Ramzi_V2` = near-total replacement). Grid `src/components/product/productGrid.tsx` `grid-cols-2 md:grid-cols-3 lg:grid-cols-4` (~:36). Results view `src/sections/searchResults/view/search-results-view.tsx`.
- **Specific defects in the diff:** negotiable / accept-offer / "my_announce" badges use `bg-gradient-to-r` with **NO from/to color stops** → render as transparent/empty gradient boxes (~:338, ~:370) — the "placeholder-heavy" look. Auction chip palette changed to pale `from-sand-100 via-sky-100 to-cyan-100` (prod was `from-[#e5d2e2] via-[#cde3f1] to-[#daf0f4]`); price changed from gradient clip-text to solid `text-foreground` (intentional, **dbcae85f**); border `border-primary/30`/`border-line-100` vs prod `border-[#846BAD]`/`border-gray-300`; shell changed to `<div>` wrapping `<Link>` with heart/share moved outside (**4bc78df7**).
- **"Vertical side banners flanking the grid" — CLARIFICATION:** on prod, `search-results-view.tsx:546` rendered `hidden md:block w-1/4 border ... rounded-lg` = a DESKTOP-ONLY framed FILTER column (not an ad banner). Ramzi_V2 moved the filter into a collapsible `SidebarFilters` sheet and replaced the flanking with inline HORIZONTAL `DisplayAdBanner` strips (`search_top`/`search_mid`). The display-ads platform is entirely new in Ramzi_V2.
- **Staging-vs-prod-main diff + regressing commits:** card look driven by the coastal repaint **48bb6db7** "platform-wide coastal repaint" + the gradient-badges-missing-color-stops + shell change; plus **b8846b60, 734ba910, 7f2986ac, 5f2c032c** (color rewrites), **4da813ff, d49ce069, 1281169c, a9f1a2a0** (size/null fixes).
- **Root cause:** the card rewrite left several `bg-gradient-to-r` badges without from/to stops (transparent boxes), softened the palette, and dropped the framed side column — cumulatively the staging grid looks placeholder-heavy vs prod's clean bordered cards + tags + framed side column.
- **Fix plan (WEB-only, toward prod):** add proper from/to color stops to the negotiable/auction/accept-offer/my-announce badges; restore recognizable tag styling (negotiable, auction) toward prod; decide the flanking column — either restore the framed filter column OR render a proper VERTICAL ad rail (`listing_sidebar` is defined but NOT rendered on search today). Not a simple revert; adjust Ramzi_V2 styling.
- **Mobile safety:** grid is `grid-cols-2` base (mobile) → `lg:grid-cols-4`; gate any layout change behind `md:`/`lg:`. The gradient-badge fix is shared across breakpoints and currently equally broken on mobile, so fixing it is safe for both.
- **Layer:** WEB-only. **RED/YELLOW:** YELLOW (cosmetic). **Needs-care** (shared card used platform-wide; verify home/related/dashboard grids; one bounded slice + z-index/visual sweep per §33.6).

## FR-7 — SAVE-SEARCH MIN-CRITERIA (button on bare category page)
- **Component / file:line (CONFIRMED from source):** `src/sections/searchResults/view/search-results-view.tsx:371` `SAVEABLE_SEARCH_PARAMS = ['search_text','cat','scat','scat2','lat','lng','dist','city','cityslug','squareslug','type','store','hasvideo','minp','maxp','properties','newp','entity_id']`; `:376` `hasSaveableSearchContext = SAVEABLE_SEARCH_PARAMS.some(k => searchParams.get(k) !== null && '')`; button gated at `:776` `{ !countryData && hasSaveableSearchContext && ... }`.
- **Root cause (CONFIRMED):** the list INCLUDES `cat` (+ `scat`/`scat2`/`type`/`store`). A bare category URL like `?cat=5` sets `hasSaveableSearchContext=true`, so the save-search button renders on a category-only page with no real search intent. (The current code already correctly EXCLUDES sort/`pg` per its own comment — so pagination/sort no longer trigger it; category/type/store still wrongly do.)
- **Fix plan (WEB-only):** require a MINIMUM real search before showing the button — i.e. at least one of: location (`city`/`lat`+`lng`) OR `search_text` OR a real filter (`minp`/`maxp`/`properties`/`hasvideo`/`newp`). Do NOT count `cat`/`scat`/`scat2`/`type`/`store` alone. Split the list into "real-search params" vs "context-only params" and compute the gate on the real-search set. Reuse the existing gate mechanism (B16 built it).
- **DO NOT DISTURB:** the S4 save-search email-consent work — `useMarketingConsentPrompt('save_search')` + the notify toggle in the save modal must remain intact. This change only narrows WHEN the button appears, not the consent flow.
- **Mobile safety:** pure conditional logic, no layout/breakpoints — mobile unaffected.
- **Layer:** WEB-only. **RED/YELLOW:** YELLOW (UX correctness, no data risk). **Quick-win.**

## FR-8 — NOTIF/MESSAGE BADGE STUCK after viewing
- **Component / file:line:** store `src/stores/useNotificationStore.ts` — `unreadCount` is DERIVED from `notifications[].isRead` (`countUnread` on setNotifications :84-91 / addNotification :93-101 / markAsRead :103-111); `messageCount` is an independent counter set only via `setMessageCount` (:113). Header reads both at `SiteHeader.tsx:39`; badges rendered `:207` (messages) / `:218` (notifications).
- **Notifications break:** opening the panel does NOT mark anything read — `notificationsModal.tsx` only renders the list, no mark-read side effect on open. Read-state clears ONLY on per-item click (`notification.tsx:35-40` → `patch('notifications/'+id+'/read')` + `markAsRead(id)`). There is NO "mark all as read" for notifications (no store action, no API endpoint — API `notification.controller.ts` has only `@Patch(':id/read')` at :107; grep confirms no notifications `mark-all-read`).
- **Messages break:** clearing exists but only on `InboxView` mount (`InboxView.tsx:77-79` → `post('messages/mark-all-read')` + `setMessageCount(0)`). Any path that views messages without mounting InboxView never zeroes `messageCount`.
- **Staging-vs-prod-main diff + regressing commit:** the store-derived-badge model + per-item-only clear landed in the notification refactor (`useNotificationStore.ts` newly added +122; `notification.tsx` +263; `notificationsModal.tsx` rewritten). No commit added a notifications `mark-all-read` or a clear-on-view.
- **Fix plan (CROSS-LAYER):** Notifications = add API `notifications/mark-all-read` (mirror the messages endpoint) + WEB call it on panel open and set all `isRead=true` in the store. Messages = wire `messages/mark-all-read` + `setMessageCount(0)` to the header message-open action, not only InboxView mount.
- **Mobile safety:** store/logic + header, no desktop-only layout — mobile benefits equally.
- **Layer:** CROSS-LAYER (web + new api endpoint for notifications; web-only for messages). **RED/YELLOW:** RED (broken feedback loop, visible on every session). **Needs-care** (new API endpoint) — messages half is quick-win.

## FR-9 — NOTIF DEEPLINK + COPY (callback request not deeplinked, unclear copy)
- **Component / file:line:** API creation `tawadoo_api_js/src/modules/leads/leads.service.ts` — callback lead → `notifySeller` (:687-711) creates notification with `notificationCode: 'leadReceived'` (:692), passes lead id only as `link = .../dashboard/leads/${lead.id}`, sets `email:false`, and does **NOT set `service` or `objectId`**. Copy is DB-driven from `ta_notification_translation` keyed by `leadReceived` (`notification.service.ts` findTranslationsByNotificationCode) — no FR/AR/EN in source (generic/absent → "didn't say what it was").
- **WEB handler:** `src/components/NotificationModal/notification.tsx:33-93` `handClickNotification` routes ENTIRELY off the `service` string (branches for Wallet/Offer/Publication/SaveSearch/Auction/MessageThread/Review) and IGNORES the API-computed `deepLink`. There is NO lead/callback branch. Since `notifySeller` never set `service`, it falls through to the final `else` → `router.push('/notification-unavailable')` (:90). Hence no deeplink to the lead.
- **Root cause (CROSS-LAYER, both wrong):** API omits the `service`+`objectId` the WEB router needs (and WEB ignores the `deepLink` it does send); WEB has no lead case.
- **Fix plan:** API — `notifySeller` sets `service: 'LeadService'` (or agreed token) + `objectId: lead.id`. WEB — add a matching branch routing to `/${locale}/dashboard/leads/${objectId}` (and/or start consuming the API `deepLink`). Copy — define/verify FR/AR/EN `leadReceived` translations that say what it is ("Nouvelle demande de rappel / callback request") per §30, founder-approved before shipping.
- **Mobile safety:** logic + copy, no layout — mobile unaffected.
- **Layer:** CROSS-LAYER (web + api). **RED/YELLOW:** RED (lead not reachable = lost revenue path). **Needs-care** (touches lead-gen; §30 copy).

## FR-10 — LEAD-UNLOCK TRANSPARENCY (no explanation how it's paid)
- **Component / file:line (CONFIRMED):** API `leads.service.ts` `unlockLead` (~:409-540): resolution order = subscription **included-allowance FIRST** (source `'included'`, 0 coins) else **coins** (`this.unlockCost`, default 10, `LEAD_UNLOCK_COST`, wallet under pessimistic_write; throws `Insufficient coins` if short). NOTE: it is TWO tiers (included-allowance, then coins) — there is no separate "free vs bonus" split. Endpoint `leads.controller.ts` `@Post(':id/unlock')` (:496-521) returns ONLY `{id,unlocked,buyerName,buyerPhone,buyerEmail,buyerMessage,status}` — it DROPS the computed `source`, `coinsSpent`, remaining balance, remaining allowance.
- **WEB UI:** `src/components/syndication/LeadsDashboard.tsx` — unlock button label `t('unlockFor',{cost:10})` (cost HARD-CODED to 10, not from API); `handleUnlock` (:56-76) fires immediately with NO pre-confirm, toasts `unlockSuccess`, and hard-codes analytics `coins_spent:10`. No post-feedback on how it was paid / coins left / allowance left. If an allowance was used (0 coins), UI still implies 10 — misleading. `LeadUnlockResponse` type (`utils/api/leads.ts:29-37`) has no cost/source/balance fields.
- **Root cause (CROSS-LAYER):** the API computes source/cost/remaining but drops them at the controller boundary; the WEB has no pre-confirm and no post-feedback and hard-codes 10.
- **Fix plan:** API — extend unlock response with `source` ('included'|'purchased'), `coinsSpent`, `coinsRemaining` (post-deduction wallet), `allowanceRemaining` (service already has these). WEB — (1) pre-unlock confirm stating cost X coins OR "free from your package/bonus, N left" + show balance; (2) post-unlock feedback using the new fields; (3) stop hard-coding cost 10 in UI + analytics. §30 FR/AR/EN copy, founder-approved.
- **Mobile safety:** logic + copy/modal, no desktop-only layout.
- **Layer:** CROSS-LAYER (web + api). **RED/YELLOW:** YELLOW (transparency/trust; money involved so verify carefully). **Needs-care** (money path; §30 copy).

## FR-11 — MARK-LEAD-STATUS FAILS ("Échec de la mise à jour")
- **Component / file:line (CONFIRMED):** API `leads.controller.ts` `@Patch(':id/status')` (:527-552), `JwtAuthGuard`, body `UpdateStatusBodyDto {status:'viewed'|'contacted'|'converted'}` (~:100, plain union, no class-validator), `verifyEntityAccess` requires `x-entity-identifier` + JWT + `validateEntityForUser`; `contacted` → `markAsContacted`. Service `leads.service.ts` `markAsContacted` (:594-620): `findOne({id, seller_entity_id: entityId})` → `NotFoundException` if not owned; **`if (!lead.unlocked) throw new ForbiddenException('Lead must be unlocked first')`** ← THE TRAP.
- **WEB request (CONFIRMED matching):** `utils/api/leads.ts:68-75` `updateLeadStatus` → `patch('/leads/${leadId}/status', {status}, {'x-entity-identifier': entityId})`; `patch` helper sets `Content-Type: application/json` + JWT via axios interceptor. Method PATCH ✓, URL ✓, path param ✓, body field `status` ✓, value lowercase `'contacted'` ✓ (no casing mismatch), header ✓. Trigger `LeadsDashboard.tsx` `handleMarkContacted` (:78-86) → catch → `toast.error(t('updateFailed'))` = "Échec de la mise à jour" (`fr.json:2913`).
- **Root cause (CONFIRMED):** NOT auth/URL/payload/casing — the web call is correctly shaped. It is a SERVER business-rule rejection: `markAsContacted` throws `ForbiddenException('Lead must be unlocked first')` when the lead is not unlocked (secondary: `NotFoundException` if `x-entity-identifier` ≠ `seller_entity_id` for a multi-entity user). The WEB catch is generic, collapsing the 403 into the meaningless "Échec de la mise à jour". The UI usually only shows the button when `lead.unlocked` is true, so the trigger is a stale/mismatched `unlocked` flag or a wrong active entity.
- **Fix plan (CROSS-LAYER; primarily WEB error-handling + POLICY):** WEB — read the API error and show a specific message (e.g. "Débloquez d'abord ce contact" for unlock-first) instead of the blanket `updateFailed`; ensure the button is never offered / is disabled when `unlocked` is false/stale (re-fetch on open). POLICY (FOUNDER STOP): is "must unlock before marking contacted" the intended rule? If YES → messaging-only WEB fix. If a seller should be able to mark contacted without unlocking → change the `if(!lead.unlocked)` guard in `markAsContacted` (business decision, surface before changing).
- **Mobile safety:** logic + copy, no layout.
- **Layer:** CROSS-LAYER (web error handling definite; api rule = founder decision). **RED/YELLOW:** RED (functional break on the prospects tab). **Quick-win** for the messaging fix; policy check needed.

---

## SHARED-COMPONENT MAP (which items MUST be one session vs independent)
- **`productCard.tsx`** → FR-6 + FR-2 (auction card visual) — SAME component, one session.
- **`search-results-view.tsx` + `filter.tsx`** → FR-3 (pagination) + FR-7 (save-search gate) both live here. Different concerns; can be one carefully-scoped session or two sequential slices on the same file — must NOT collide with a parallel session that also writes this file.
- **`product-details-view.tsx` + ads slot-config/DisplayAdBanner** → FR-5 (detail layout) + the ad-slot variant; FR-6's side-banner decision also touches the ads layer → keep FR-5 + FR-6 side-banner coordinated.
- **Notifications (`useNotificationStore`, `notification.tsx`, `notificationsModal.tsx`, api notification/leads)** → FR-8 + FR-9 overlap the notification store/handler and the leads notification payload. Group.
- **Leads (`LeadsDashboard.tsx`, `utils/api/leads.ts`, api `leads.service.ts`/`leads.controller.ts`)** → FR-10 + FR-11 (+ FR-9 api side) all in the leads subsystem. Group.
- **Independent:** FR-1 (LiveAuctionStrip, homepage only), FR-4 (chat tree + SocketProvider).

## PROPOSED BUILD-SESSION GROUPING + ORDER (functional before cosmetic)
Order by risk: R0 (functional RED) → R1 (lead-gen/notif RED) → R2 (visual YELLOW) → policy-gated last.

1. **S-CTO-25 · SEARCH FUNCTIONAL (RED, R0)** — FR-3 pagination + FR-7 save-search gate (same files `search-results-view.tsx`/`filter.tsx`; do them together to avoid collision). WEB-only. Reason: highest-impact functional break across all listings + a clean quick-win in the same file.
2. **S-CTO-26 · CHAT RECOVERY (RED, R0)** — FR-4. WEB primary + verify staging ALB WebSocket upgrade FIRST (runtime QA). Reason: core messaging dead; must reproduce live before patching the rebuilt socket. Independent files.
3. **S-CTO-27 · LEADS SUBSYSTEM (RED/YELLOW, R1, CROSS-LAYER)** — FR-11 mark-contacted messaging (+ founder policy on unlock-first) + FR-10 unlock transparency (api response extension + web confirm/feedback). Reason: same leads files/subsystem; money + §30 copy; founder decision needed.
4. **S-CTO-28 · NOTIFICATIONS (RED/CROSS-LAYER, R1)** — FR-8 badge clear (new api `notifications/mark-all-read` + web clear-on-view; messages half web-only) + FR-9 callback deeplink+copy (api `service`/`objectId` + web lead branch + §30 copy). Reason: shared notification store/handler + leads notification payload; §30 copy.
5. **S-CTO-29 · CARD + AUCTION VISUAL (YELLOW, R2)** — FR-6 card styling/tags + FR-2 auction card render (same `productCard.tsx`) + FR-2 subscribe/notify (cross-layer, may split to its own R2 if the endpoint/trigger is sizable). Reason: one shared component; run a z-index/visual sweep (§33.6).
6. **S-CTO-30 · DETAIL LAYOUT + PINK STRIP (YELLOW, R2)** — FR-5 desktop detail layout + ad-slot vertical variant, and FR-1 LiveAuctionStrip restyle/guard. Reason: both cosmetic desktop-facing; FR-5 touches the ads layer that FR-6 also considered — coordinate the side-banner decision across S-CTO-29/30. Mobile-safe (all `lg:`-gated).

**Founder decisions to surface (STOP) before the build sessions:**
- FR-2: keep showing not-started auctions with a subscribe CTA, or hide them until subscribe exists? (build size depends on this)
- FR-11: is "must unlock before mark-contacted" the intended rule? (messaging-only vs rule change)
- FR-5/FR-6: do you want real VERTICAL side ad banners, or the clean prod look (no side banner)?
- All §30 copy (FR-9, FR-10) approved in FR/AR/EN before shipping.

## ACCEPTANCE / STOP-CONDITION CHECK
- All 11 items: component file:line ✓ · staging-vs-prod-main diff + regressing commit (where a diff exists; FR-1/FR-2/FR-8/FR-9/FR-10/FR-11 are additions/behavior gaps, noted) ✓ · root cause ✓ · fix plan (web/cross-layer flagged) ✓ · mobile-safety ✓ · RED/YELLOW + quick-win/needs-care ✓.
- Build-session grouping + order (functional before cosmetic) ✓. FR-2 subscribe/notify existence stated (PARTIAL) ✓. Cross-layer items flagged for api (FR-2, FR-8, FR-9, FR-10, FR-11; FR-4 infra/ALB) ✓.
- NO code changed. HEADs unchanged: web Ramzi_V2 @ 8b0a815f, api Ramzi_V2 @ 8d55a20 (verified). Only this evidence file written.

**Runtime caveat (honest):** all findings are source + git verified. FR-4 (socket `connected` on staging desktop + ALB WS upgrade) and the exact live rendering of FR-1/FR-5/FR-6 were NOT browser-confirmed in this read-only session — flagged as the first step of their build sessions per tawadoo-runtime-provider-qa.
