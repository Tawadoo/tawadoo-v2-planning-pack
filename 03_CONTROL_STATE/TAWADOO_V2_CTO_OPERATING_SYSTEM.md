# TAWADOO V2 — CTO OPERATING SYSTEM + LIVE CTO STATE
**Role:** Permanent CTO / Principal Architect / Release Commander. **Installed:** 2026-09-05 (Brain B22 → CTO mode, founder directive).
**Mission:** Get Tawadoo V2 into a coherent, safe, verified, production-ready state and replace the current live V2 across **web + app + ChatGPT/MCP**. SHIP + PROPERLY REFACTOR (where touched) + VERIFY + CUT OVER. Not patchwork, not endless planning, not full rebuild.

**Operating laws (compact):** RED (block launch) / YELLOW (small high-leverage) / BLUE (post-launch) · R0/R1/R2/R3 for architecture · BUILT≠LIVE · verify from source+runtime · browser-verify all UI · staging-first, prod protected · no blind merges, no panic reverts · I drive the sequence and hand the founder ONE next 10/10 prompt · security may interrupt · deferred stays deferred unless founder un-defers or it becomes a blocker · no reconciliation loops — history is context, execute.

**SESSION-ID LAW (founder-mandated 2026-09-05):** EVERY session gets a permanent canonical ID `S-CTO-<n>`, surfaced at the top of the prompt, in the five-line opener, and in the evidence filename + header. Never issue a prompt without an ID. The SESSION LEDGER below tracks every one so we never lose track.

**READ→BUILD PAIRING LAW (founder-mandated 2026-09-05):** every read-only DIAGNOSIS/AUDIT session is immediately paired with its BUILD/WRITE session. When a read-only session lands, I audit its evidence and hand the founder the build prompt in the SAME reply — no re-audit, no re-asking, no circling. Diagnosis exists ONLY to make the very next build session source-true. A read-only session is never the end of a thread; the fix that follows it is.

**BUILD-NOT-READ LAW (founder-mandated 2026-09-06 — ENOUGH READING):** default to BUILD/WRITE sessions that INVESTIGATE-THEN-FIX in ONE shot (the 360 happens inside the build session, then it fixes + deploys + proves). Do NOT author a standalone read-only audit UNLESS: (a) the change is genuinely risky/ambiguous and a wrong fix could regress (then a bounded read-first is justified) OR (b) the DB/live truth is unreachable and a fix can't be scoped without it. When a read-only IS fired, we do NOT move to any other topic until its WRITE/FIX session is fired — reading is never a stopping point, the fix that follows it is. Where the root cause is ALREADY source-known (e.g. FR-7 save-search `cat` gate, FR-11 mark-lead endpoint), skip the audit and go straight to the build. Every audit must end with its fix fired. No read-read-read loops.

**360-INVESTIGATION-FIRST LAW (founder-mandated 2026-09-06 — no time for regressions/rework/mess):** EVERY session (build or read-only) MUST open with a thorough 360 source investigation BEFORE touching anything: trace the full path across all relevant repos (api/web/bo/mcp) + DB + live/runtime where relevant; verify EVERY name (route, endpoint, screen, event, component, DB column, function) from SOURCE with file:line — a name from a report/doc/memory is a HYPOTHESIS, never truth, until grep-confirmed (earned: Brain carried a hallucinated `my-publications` route from a stale doc; real route = `publications/mine`). Map callers, consumers, downstream, and the current behavior. Only after the 360 is the fix/change designed. No guessing, no partial reads, no "the report said." Regressions and rework cost more than the investigation. Every prompt's WORK section must start with this 360 and every claim in evidence must be source-cited or tagged UNVERIFIED.

**BUSINESS-TRUTH-FIRST LAW (founder-mandated 2026-09-06 — earned: Kiro kept re-asking core business it should KNOW):** `TAWADOO_BUSINESS_TRUTH_2026_09_06.md` is the canonical, source-verified answer to Tawadoo's core business (moderation/human-in-the-loop, jobs/services taxonomy, distribution/feeds/guards, money, sovereignty, red lines). EVERY prompt author + session reads it FIRST. If a session would ASK the founder something answered there, answer it from the file instead. Only surface GENUINE undecided business/policy questions. Asking the founder a question already answered in source/this file is a prompt-authoring failure.

**DISTRIBUTION / FEED LAW (founder-mandated 2026-09-06 — SERIOUS, earned by real Google Merchant SUSPENSION history):** Distribution to Google/Meta/TikTok/etc. is a HIGH-RISK, HISTORY-LADEN surface — there is a whole history of feed guards + a GMC suspension (see `GOOGLE_MERCHANT_APPEAL_2026_08_23.md` + the feed-quality-gate + eligibility guards). ANY session touching feeds/syndication/eligibility MUST: (1) read that history + the existing guards FIRST (360), (2) never weaken a guard that exists to prevent a provider ban, (3) treat a provider suspension as the real cost — a mislabeled/ineligible/mismatched item reaching a provider can ban the account.
**F2 — FOUNDER DECIDED 2026-09-06: MANUAL MODERATION IS INTENDED — human-in-the-loop STAYS ALWAYS (X6 sacred).** A human approves a listing for PUBLIC VISIBILITY before it goes live — never removed, never auto-approved. This is SEPARATE from distribution: once a listing is moderated/eligible, distribution is silent + automatic (routing, not a 2nd approval). So F1 (feed drops eligible photoless) is a BUG to fix; F3 flag-contract convergence is fine to do; but the moderation gate itself is NOT to be automated away.
**FOUNDER DISTRIBUTION POLICY (locked):** SILENTLY distribute EVERY eligible listing from the start (first months) to maximize traffic before paid kicks in — free, paid, boosted or not, ALL get distributed **as long as they are ELIGIBLE and fall under the RIGHT feed/channel**. Do NOT communicate distribution status to users (not "free", not "boosted", not distributed-or-not — silent). Eligibility + correct-feed-routing is the ONLY gate; boost/paid affects ranking/campaign, not whether an eligible listing is distributed. Therefore the F1 photoless-drop bug is DIRECTLY against this policy — an eligible photoless job/service that should be silently distributed is instead silently DROPPED. §50 safe-default: never block/hurt a user or silently kill an eligible listing's reach.

**ARCHITECTURE-MAP-FIRST GATE (founder-mandated 2026-09-06 — earned: S-CTO-18web began building before realizing indexing lives in OpenSearch, an api-side pipeline; a web session was working half-blind):** the 360 is NOT complete until the session has drawn the END-TO-END DATA-FLOW MAP for the feature it touches, ACROSS repos and systems, and confirmed WHICH LAYER OWNS EACH STEP. For any feature, the session must state, from source (file:line), the full chain — e.g. for listings: create-form (web) → publish endpoint (api) → DB write (system of record) → **indexer → OpenSearch (projection)** → search-read endpoint (api) → search UI (web) → card/detail render (web) — and identify which steps are in its writable repo vs another repo/system. If the real fix or the real cause lives in a layer OUTSIDE the session's writable repo (e.g. a web session finds the truth is in the api OpenSearch indexer), the session MUST STOP and report it as a cross-layer/coordination item — it must NOT build a partial fix in the wrong layer. The prompt's 360 MUST name the expected owning layer for each step so the session knows the architecture BEFORE it edits, not after. Projections (OpenSearch index, feeds, Amplitude, lake) are rebuildable and are NOT the system of record — never fix a projection when the record or the writer is the real cause. This gate is a hard prerequisite for any WORK; a session that discovers the architecture mid-build is a prompt-authoring failure to be flagged.

**UNKNOWN-EVENT = BUILD-OR-ASK LAW (founder-mandated 2026-09-05):** an undeclared/unknown event is NEVER silently ignored AND never silently accepted. Every one is classified: (A) maps to something ALREADY in the UI → WIRE + declare it (real behavior we're just not capturing); (B) NO current UI → Kiro describes it to the founder in plain English (what it is, what it captures, why it matters) and the founder APPROVES / DISCUSSES / REJECTS — approved → build list, rejected → prune; (C) genuine junk/dup → propose prune. The reject-unknown validation must SURFACE unknowns for this triage, not drop them. Nothing about a user behavior is discarded without the founder seeing it.

**NAME-COLLISION HARD GATE (founder-mandated 2026-09-05 — earned by real DB/BO mess):** Kiro has historically created NEW names for functions / DB columns / event properties / IDs / endpoints / BO resources that ALREADY EXISTED under a different name, because it did not check first. This caused duplicate columns, drift, and unusable data. NEW RULE, blocking on every build session: **before creating ANY new named thing (DB column/table, event property key, function/method, ID field, endpoint, BO resource, entity, migration name), the session MUST first grep/inspect the codebase + the live schema for an existing name that already serves that purpose — and REUSE it.** A new name is allowed ONLY after the session records, in its evidence, the search it ran and the proof no equivalent exists. Reusing a slightly-wrong existing name beats inventing a clean-looking duplicate. For DB/BO specifically: read the actual table columns (`information_schema`/entity file) and the actual BO resource list BEFORE proposing any add. If unsure whether two names mean the same thing → STOP and surface it as a founder decision, do not create a parallel one. This gate must appear in every future prompt's NON-GOALS + VERIFICATION.

### 🟢 RESUME CHECKPOINT — 2026-09-05 (founder resting; S-CTO-12 QA'd + accepted seller-side)
### 🟢 RESUME CHECKPOINT — 2026-09-06 (Brain B23 booted; founder said PARK ALL + PAUSE; NOTHING IN FLIGHT)
**Read this first on return. Full detail in the ledger + parked register below.**
- **I AM BRAIN B23.** Booted by reading the full B22 conversation (all 164 founder msgs) + this operating system + business truth + master queue. Same laws, same North Star, no re-planning. Continue S-CTO numbering (last = S-CTO-24).
- **LIVE GIT ✅ VERIFIED 2026-09-06:** web `8b0a815f` · api `8d55a20` · bo `07b3a84` · mcp `d8efb4a` — all `Ramzi_V2`, synced. **NOTHING IN FLIGHT.**
- **S-CTO-23qa QA'd + ✅ ACCEPTED by B23** (read-only): human-in-the-loop moderation→visibility chain PROVEN live (publish→unverified=invisible; founder BO Verify→visible). 3 core claims source-confirmed (D1 anomaly / D2 vector-filter hardening / drift). Full row in ledger.
- **R0 SYNTHETIC-CLEANUP — DISMISSED BY FOUNDER 2026-09-06:** "no panic, it's just me using staging, it's dev env." Synthetic listings (`bfa8710e` etc.) are NOT a concern; do NOT chase cleanup. Removed from priorities.
- **NEXT (staged, NOT fired — founder paused):** S-CTO-25 (functional frontend fix: FR-3 pagination + FR-11 mark-lead + FR-8 badge/mark-all-read + FR-9 notif deeplink, web+api) + S-CTO-26 (FR-1 remove pink + FR-7 save-search gate, web). Prompts written: `KIRO_EXEC_PROMPT_S_CTO_25_FUNCTIONAL_FRONTEND_2026_09_06.md` + `KIRO_EXEC_PROMPT_S_CTO_26_REMOVE_PINK_SAVESEARCH_2026_09_06.md`. Both source-verified. Safe pair (different files, one browser). Awaiting founder GO.
- **D1 (isModerated-at-publish trace) + D2 (vector-leg isVerified filter hardening)** = small tail items to fold into the moderation/api work when touched — NOT their own sessions.

---
#### (superseded) 🟢 RESUME CHECKPOINT — 2026-09-05
- **S-CTO-12 landed + QA'd = ✅ ACCEPTED (seller side).** S-CTO-R-RENDER / S-CTO-15 items below were the then-next; superseded by the frontend-regression wave.

**From S-CTO-15 + S-CTO-R-RENDER (2026-09-06) — open items, nothing buried:**
- **B-LIST — FOUNDER DECIDED 2026-09-06:** `store_search` + `store_categories_updated` = APPROVED to build LATER (with their surfaces, when store-search is built); 3 redundant `smart_view_*` = REJECTED (prune). Closed.
- **PHOTOLESS FALLBACK — FOUNDER DECIDED 2026-09-06:** for job/service listings with NO image, the default visual = the CATEGORY ICON from the categories carousel (not a generic placeholder.png). Meaningful + consistent. Applies to seller "Mes ventes" thumbnail AND any buyer surface that showed a gap. → build in S-CTO-17web.
- **FLAG-A (RED — must verify):** photoless services may be ABSENT from public search index (`/publications/search` returned 0 photoless). If real, buyers can't find them → undercuts the job/service feature. → api/search diagnosis (S-CTO-16 or its own). SINGLE UNVERIFIED OBSERVATION — confirm before believing.
- SELLER-THUMBNAIL (YELLOW, 1-line): `Mes ventes` photoless thumbnail 404s (`placeholder.png` missing on staging). → tiny web fix, fold into S-CTO-16.
- I6 (YELLOW): Amplitude tracking-plan registration for the 28 canonical events NOT done → S-CTO-18.
- I1 (YELLOW): DEFAULT partition holds 8 events + dormant [ALARM:DEFAULT_PARTITION] → partition-routing probe.
- I7 (YELLOW): DB row-parity verified via CloudWatch logs not psql (ECS-Exec down, OPEN-U3-4).
- I2/I13 corrections: 6 pre-existing failing suites on clean HEAD (CI may not catch); prior S-CTO-13a/14 event proposals partly wrong — S-CTO-15 triage is corrected truth.
- Smart View card / MCP card / BO listing render UNREACHED this sweep → fold into the Smart View completion + MCP sessions.
- HOUSEKEEPING: S-CTO-R-RENDER left untracked test artifacts under `tests/e2e-staging/scto-r-render/` (no product code) — commit as regression guard OR remove for zero footprint (founder/Brain call).

**DISTRIBUTION FINDINGS from S-CTO-22api (2026-09-06) — some ban-adjacent, nothing buried:**
- **D-FIX ✅:** feed no longer drops truly-zero-image listings (`leftJoin`, commit `8d55a20`). Shape-1 done.
- **D-1 (verify owed):** F1 has TWO shapes — a video/cover counts as "has image row", so a photoless-shoes listing already got through; only literally-zero-row listings were dropped. Real job/service listing shape UNVERIFIED (DB locked) → S-CTO-25api seed+observe.
- **D-2 (self-corrected error):** the session first wrongly said "staging feeds empty" (checked wrong bucket); CORRECTED — staging feeds DO generate every 6h, current to yesterday. Recorded so the false "empty feeds" claim isn't carried.
- **D-3 🟡 (config smell, ban-adjacent):** staging writes feeds into a PROD-named bucket `tw-prod-media-storage-prod` — mixing staging content into a prod-named location. Config review owed; did NOT touch (out of scope).
- **D-4 🟡 (attribute strip — investigate):** since early Sept the feed stopped emitting `condition: new` even for genuinely-new listings ("Neuf"). Safe-by-design (avoids the used-as-new ban) BUT may strip a valid attribute from many legit listings → investigate whether genuinely-new should carry the tag.
- **D-5 🟡 (silent gap?):** only Meta feeds exist on staging — no Google (correct, guard excluded the photoless item) but ALSO no TikTok/ChatGPT files. May be expected, may be a gap → verify.
- **D-6 (CI gap):** CI does NOT run syndication tests (only analytics/amplitude) → feed regressions won't be caught automatically. Widen CI (careful — frozen CI files).

**SEARCH-INTELLIGENCE 360 + BUILD (founder-flagged 2026-09-06 — planned across brains, real unit):**
- **THE GAP:** the BO currently shows KEYWORD aggregates only. Founder wants PROPER search intelligence: WHAT users search, WHERE from, HOW — across image / category / language / location, not just keywords — feeding training + forecasting + reporting (both-sides supply+demand understanding).
- **HISTORY (source-found):** `CTO_BEHAVIORAL_INTELLIGENCE_GLOBAL_RESEARCH_2026_08_20.md` (the ~170-signal behavioral plan: camera-permission context = image-search intent #17, seasonal demand by city #25, inferred_tech_level from image-search, etc.) + `TAWADOO_V2_AI_VISION_COMPLETE_2026_08_19.md` (image_search events #336 `image_search_initiated`, #337 `image_search_results_shown`; Titan 1024-dim embeddings on ~210K images; image search "12 similar near you"). The `searchEnrichment` api module EXISTS (activity-sieve, AI category extraction, cross-lingual tags, Darija lexicon ~639 entries, hybrid keyword+vector, image→tag, geo-normalization, training-data logger) — engine built, NOT surfaced in BO.
- **THE UNIT (queued, NOT yet fired — needs founder-approved execution plan):** a 360 (api searchEnrichment + BO search read-surface + lake events) → then BUILD the BO intelligence read-surface (image/category/language/location dimensions, not keywords) + ensure the search-intelligence events land + wire the reporting. Spans api + BO. This is a MEDIUM-LARGE unit — plan it as its own execution, per build-not-read (the 360 + build can be one investigate-then-build session for the read-surface, but the scope is big → likely 2 sessions: api events/enrichment surfacing + BO read UI). Founder wants the proper execution PLANNED.
- **CROSS-REF:** ties to the moat (Bayesian model feeds on this), the event map (search events in the 492), and the founder's both-sides supply/demand goal.

**M-ZONE — THE MODERATION → SUPPLY/DEMAND BLACK ZONE (surfaced by S-CTO-18web, the real thread we kept circling):**
- **THE CORE INSIGHT:** a freshly-published listing is `isModerated=false` → EXCLUDED from public search AND from every distribution feed. So "photoless doesn't show" was never a render bug — it's the moderation gate. Moderation is BO/admin work. This gate is the entry to the whole supply/demand visibility chain.
- **M-C2/C3 (RED-adjacent, needs api+DB+BO investigation):** WHY do fresh (photoless) publishes stay unmoderated/unindexed? Who/what sets `isModerated` vs `isVerified` (TWO different flags, different effects)? Is staging moderation even ON? (inferred from API, not psql — DB IP-locked, ECS-Exec down OPEN-U3-4.)
- **M-C2 (BO):** there is a real moderation QUEUE + audit log in `admin_bo_tawadoo` never opened this program — map it.
- **M-C1 (feed, UNCONFIRMED — Brain partly refuted):** eligibility service uses `leftJoinAndSelect('pub.images')` (line 54, NOT inner → photoless NOT dropped there). BUT a specific Google-Merchant FEED FORMATTER may still inner-join on images → would silently drop photoless from that channel. Needs the api feed-formatter checked (read-only). Supply-leak risk IF real.
- **M-C6 (product decision, founder):** a listing owner cannot preview their OWN unverified photoless detail page (404s). Intended or should owner see their own pending listing?
- These items are the bridge to the founder's long-stated goal (understand supply+demand both-sides, feed the model, serve advertisers). The moderation/index/feed chain is where a listing becomes DISCOVERABLE — mapping it = mapping the supply funnel.
- **DONE today (verified):** S-CTO-3 money identity · S-CTO-5 video · S-CTO-7 auth diagnosis · S-CTO-8 job/service backend · S-CTO-9 auth security · S-CTO-11 draft finisher (product-form-v2 now CLEAN) · S-CTO-14 event map (492: 157 landing/128 wired-not-fired/207 paper) · S-CTO-13a naming reconciliation.
- **FOUNDER DECISIONS LOGGED:** PROD = HOLD (leak fixes at cutover, don't touch prod) · D1 job/service image-optional = YES · D2 Maps key = defer to cutover · A-DEC1 prod test accounts = leave · N-DEC1 context_switch = ADD as view-switch · N-DEC2 = ai_generation_completed / keep price_suggestion_shown.
- **STILL PENDING FOUNDER:** §28.5 which commits merge to Ramzi_V2 · the S-CTO-15 B-list (coming) · Bayesian 5 decisions (later, at M-phase).
- **BIG FINDS not to lose:** MCP is a separate event island → local SQLite /tmp, NOT feeding the sovereign lake (Commandment-2 gap → S-CTO-17, founder-gated, external contract). Money funnel wired but never landed (needs one real staging transaction to prove). "Reject-unknown" contract was never built (root cause of naming drift → fixed in S-CTO-15).
- **REMAINING RED before refactor:** the my-publications 500 (S-CTO-13) · §28.5 merge · cohort crash · media guard · app staleness · MCP contract inventory · auth flow (S-CTO-10).
- **NEXT AFTER S-CTO-12 QA + S-CTO-15:** S-CTO-16 emitter wiring/renames (dark surfaces + money-funnel landing proof) · S-CTO-17 MCP→lake (founder-gated) · S-CTO-18 DB parity + BO gap · then remaining RED · security gate · REFACTOR (R0–R6) · MOAT/Bayesian (M0–M5) · cutover. Full map in `TAWADOO_V2_MASTER_QUEUE.md`.
- **MACHINE:** max 2 sessions, prefer 1 writer + 1 reader, never 2 browser sweeps. **PROD:** never touched; Ramzi_V2 = truth; staging = future prod in days.
- **RE-ENTRY:** on return, if S-CTO-12 report is ready → paste it, Kiro QAs + updates checkpoint + fires S-CTO-15. If not ready → wait for it. Nothing auto-fires.

### 🔴 FRONTEND REGRESSION REGISTER (founder-reported 2026-09-06 w/ screenshots — SERIOUS, user-facing; staging=broken, prod-main=the GOOD reference to restore toward)
**Founder rule for ALL of these: prod-main is the design/behavior reference the founder LIKES; staging regressed. Restore toward prod-main quality. DESKTOP is broken; MOBILE is fine — DO NOT break mobile while fixing desktop.**
- **FR-1 PINK RECTANGLE (🔴) — FOUNDER DECIDED 2026-09-06 = REMOVE ENTIRELY (not restyle):** the full-width pink/magenta gradient bar ("Auctiontisxsatr listing / زايد الآن") on the listing grid. Founder doesn't know what it is or why it renders → REMOVE it completely. Fix session must FIRST confirm from source WHAT the component is + where it's rendered + whether anything real depends on it (don't kill a live feature by accident), then remove its render entirely. If it turns out to be a legit-but-broken feature, STOP and report before removing — but default = remove.
- **FR-2 AUCTION CARD BROKEN + policy (🔴):** auction card is badly rendered. FOUNDER RULE: an auction that HASN'T STARTED must NOT be displayed AT ALL, UNLESS the user can SUBSCRIBE to be notified when it starts (at least email + in-app notification). Not-started-with-no-subscribe = hide it. (needs the notify/subscribe path — check if it exists.)
- **FR-3 PAGINATION BROKEN (🔴):** cannot go from page 1 → page 2 in listings (across the board). Core browse broken.
- **FR-4 CHAT/MESSAGES BROKEN (🔴):** messages in chat don't work. Core communication broken.
- **FR-5 PRODUCT DETAIL DESKTOP LAYOUT (🔴):** desktop detail page has huge empty space + tiny centered content; the display banner is a HORIZONTAL "Grow your business" strip. Should be like prod-main + the display banner VERTICAL on the side (like prod-main's dual side AD banners). Mobile is fine — do NOT touch mobile.
- **FR-6 PRODUCT CARDS UGLY (🟡→🔴):** staging cards are ugly/placeholder-heavy; prod-main cards are clean with proper tags (negotiable, auction) + vertical side AD banners. Restore toward the prod-main card style + tags. Founder explicitly prefers prod-main cards.
- **FR-7 SAVE-SEARCH RENDERS WITHOUT MINIMUM CRITERIA (🔴, founder-flagged 2026-09-06 w/ screenshot — ROOT CAUSE SOURCE-CONFIRMED):** the "Sauvegarder la recherche" button renders on a bare CATEGORY page (`?ob=desc&cat=14`, 0 results, no location). Founder rule (from history — S4 save-search planning + the search-behavior-tracking plan): save-search must require a MINIMUM real search (at least a LOCATION — city/lat/lng — or `search_text`), NOT just a category browse. **CAUSE:** `search-results-view.tsx:371` `SAVEABLE_SEARCH_PARAMS` INCLUDES `'cat'` (+ `scat/scat2/type/store`), so `hasSaveableSearchContext` (`:375`) is true on a category-only URL. **FIX:** tighten the gate — a category/type/store alone must NOT qualify; require location OR search_text (or a real filter like price/properties) as the minimum. Reuse the existing `hasSaveableSearchContext`/`SAVEABLE_SEARCH_PARAMS` (do NOT invent a new gate — B16 already built this; it just regressed/too-loose). Tie to the search-behavior-tracking plan (history: the whole search-behavior-tracked-properly conversation). §30 no copy change. Regression guard: assert button hidden on cat-only, shown on location/text search.
- **FR-8 NOTIFICATION BADGE STUCK (🔴, founder-reported 2026-09-06):** the unread count badge (message/notif) stays even after the user opens/discovers the notifications. `SiteHeader.tsx:39` reads `unreadCount`/`messageCount` from `useNotificationStore` — `markAsRead`/count-clear not firing on view. Fix: clear the badge when the user opens the notif/message panel (mark-read). Regression guard.
- **FR-9 NOTIFICATION DEEPLINK + COPY (🔴):** a test "callback request" notif landed but was NOT deeplinked to the lead + the copy didn't say what it was — founder had to hunt for it. Notifications must carry a clear title/body AND an action-URL that deep-links to the exact lead/inbox item. §30 copy (FR/AR/EN). Fix the notif payload's `actionUrl` + message + the client deeplink handler.
- **FR-10 LEAD-UNLOCK TRANSPARENCY (🔴 — UX/trust, NOT a broken unlock):** unlocking a lead did not tell the seller HOW it was paid. SOURCE TRUTH (`leads.service.ts:402-409`): unlock resolves (1) FREE/BONUS quota first, THEN (2) coin deduction `LEAD_UNLOCK_COST` default 10 coins. So it's free-if-quota-else-10-coins. The UI must PROMPT/EXPLAIN before+after: "this lead costs X coins" or "free from your package/bonus (N left)", show balance, confirm. Seller must understand the lead-gen economics. §30 copy.
- **FR-11 MARK-LEAD-STATUS FAILS (🔴, screenshot: "Échec de la mise à jour"):** "Marquer comme contacté" on the Prospects/Contacts inquiries tab fails. Endpoint = `PATCH /leads/:id/status` (`leads.controller.ts:528`, statuses viewed/contacted/converted). Diagnose why the PATCH fails (auth? entity scope? payload? — could be api or web). A seller CANNOT mark a lead properly — core lead-gen workflow broken. Fix + let seller mark viewed/contacted/converted cleanly.
- **LEAD-GEN MODEL (source-captured, for the fix session — business-truth):** leads listed per seller entity with locked/unlocked visibility; unlocked = full contact shown; unlock cost = free-quota-then-10-coins (`LEAD_UNLOCK_COST`); statuses viewed→contacted→converted (`PATCH /leads/:id/status`); call-contact recorded (`leads.controller.ts:446`). Lead-gen categories = LEAD_GEN_CATEGORY_IDS (real-estate + jobs/services). The whole flow must be UNDERSTANDABLE to the seller (how a lead arrives, what it costs, how to mark it).
- **APPROACH (per §regression-guard + business-truth):** DIAGNOSE first from source — compare staging web against prod-main (`origin/main` / prod build) for these components; find what changed/regressed; fix toward prod-main; each fix gets a regression guard (§33); browser-verify desktop AND mobile (never break mobile). Classic View sacred (FACE-001). NOTE: save-search history = B16 built `hasSaveableSearchContext`/`SAVEABLE_SEARCH_PARAMS` + S4 consent opt-in (`KIRO_EXEC_PROMPT_SYNC_FIX_S4_SAVE_SEARCH_EMAIL_2026_09_03.md`) — do NOT disturb the S4 consent/unsubscribe work when tightening the render gate.

### SESSION LEDGER
| ID | Name | Status | Evidence file |
|---|---|---|---|
| S-CTO-1 | CLOSE-U3 Live Diagnosis (read-only) | ✅ ACCEPTED 2026-09-05 — verdict (b): money events reach Amplitude but tagged with ENTITY id, not USER id. 18/19 emitters wrong; coin wallet correct. Delivery healthy (0 retry/0 DLQ). Fix = identity (RED/R1). | CLOSE_U3_DIAGNOSIS_EVIDENCE_2026_09_05.md |
| S-CTO-2 | Create-Form Consolidation (wizard+legacy→one) | ⚠️ FINISHED — INCOMPLETE 2026-09-05: built save-as-draft + autosave-recovery + unified photo same-item check into v2 (passes tests locally), but NOT committed / NOT browser-verified / NOT live-confirmed. Correctly did NOT delete legacy form (founder "don't delete" + hard-to-undo). Work sits uncommitted in tree. Surfaced 3 founder decisions (autosave-no-photo, unverified-check policy, delete-legacy). → finisher = S-CTO-4. | CREATE_FORM_CONSOLIDATION_EVIDENCE_2026_09_05.md |
| S-CTO-4 | Create-Form Draft/Autosave FINISHER — commit + deploy + browser-verify the S-CTO-2 draft work on staging (NO deletion) | PARKED (founder refocused to video-only) | CREATE_FORM_DRAFT_FINISHER_EVIDENCE_2026_09_05.md |
| S-CTO-5 | AI-LISTING JOURNEY INTEGRITY — video (upload+record) + full-chain proof | ✅ MOSTLY ACCEPTED 2026-09-05 (zero product code changed): video upload+record, add-time prohibited+same-item scan, no duplication, re-scan on add, car, auction, mobile+AR-RTL, dual-engine decode — ALL proven live. 1 HIGH break needs founder decision (O1 job/service image rule, server-side). 9 open items (O1–O9) + 5 pre-existing (P1–P5) logged below. | S_CTO_5_END_SESSION_REPORT_2026_09_05.md |
| S-CTO-6 | ~~SIGNUP-REGISTER narrow MX fix~~ RETRACTED — built on an unverified single-line report (OPEN-U3-6). Source shows EmailHygieneService.validate() is called at register (auth.controller:502) but the MX-DNS root cause is UNCONFIRMED, and there are MANY auth gates (email register, phone login/signup, email OTP, JWT, password-reset, lockout, OTP rate-limiter, identifier-change, setup-token). Replaced by S-CTO-7 (read-only auth-gates diagnosis). | — |
| S-CTO-7 | AUTH-GATES 360 DIAGNOSIS (read-only) — 3 signup methods + gates | ✅ ACCEPTED 2026-09-05 (read-only, both repos clean). Backend/API well-proven: all 3 signup methods (email · WhatsApp OTP · SSO), login, forgot-password, JWT guards tested live. NOT verified: ALL frontend auth screens (biggest gap), DB writes, BO auth, email-OTP round-trip, SSO happy-path, reset round-trip, lockout, refresh. 2 ESCALATIONS + 4 known issues + 2 founder decisions logged below. Its "S-CTO-8..12" follow-up numbering IGNORED (collides); Brain renumbers. | S-CTO-7_END_SESSION_REPORT_2026_09_05.md |
| S-CTO-3 | CLOSE-U3 BUILD — money-event identity fix (stamp user_id, keep entity id as property) | ✅ ACCEPTED 2026-09-05 — commit `ab37f4b` Ramzi_V2. All ~18 money emitters stamp acting user; entity id kept as property; no new names; no pipeline change. Proven live: boost purchased, 1000 coins, confirmed in sovereign DB + Amplitude under real user id. Zero regressions. R-1 CLOSED (forward-fix). 11 open items parked below. | S_CTO_3_SESSION_REPORT_2026_09_05.md |
| S-CTO-8 | JOB/SERVICE IMAGE-OPTIONAL (server) | ✅ BACKEND DONE 2026-09-05 (commit part of api, Ramzi_V2): API lets job/service publish w/o image, physical still requires; proven live + cleaned up; no DB/infra change; fixed a 500 (zero-image-vector crash in dup-detection). ⚠️ BATCH INCOMPLETE — WEB FORM still forces image for all → real seller still blocked. Web side = S-CTO-12 (its report mislabeled it "S-CTO-10" — IGNORED, ledger wins). Flagged: edit-path same rule (queued), moderator-approve image-search refresh throws (1-line, queued), moderation-off path untested, publication tests not in CI, staging DB secret "prod"-named footgun. | S_CTO_8_END_SESSION_REPORT_FOR_BRAIN_2026_09_05.md |
| S-CTO-9 | AUTH SECURITY FIXES (rate-limit spoof + prod error-leak) | ✅ DONE 2026-09-05 — commit `a400393` Ramzi_V2 (7 files, trust-proxy + shared IP helper). Rate-limit spoof CLOSED, proven live (2 limiter types + Chromium+WebKit login). A-ESC2 CLOSED. A-ESC1 root cause = **prod running an OLD build** → fix is a PROD REDEPLOY = ⛔ FOUNDER DECISION (approve cutover / hold; staging already fixed). Flagged: trust-proxy is GLOBAL (changes req.ip for analytics/geo IP-hashing too — likely improvement, verify downstream), BO+MCP behind same ALB may share the spoof pattern (read-only audit owed), send-email-otp throttler 400-masks-429 (partial), 1 synthetic staging user left. | S_CTO_9_END_SESSION_REPORT_FOR_BRAIN_2026_09_05.md |
| S-CTO-10 | AUTH FLOW REDESIGN (identifier-first: login=password no-OTP · forgot=OTP required · signup=OTP) + mandatory web research + end-to-end (events/training/DB/BO). [A-FLOW1] | QUEUED (needs research; touches auth screens — confirm clear of product-form-v2 tangle first) | — |
| S-CTO-11 | DRAFT/AUTOSAVE FINISHER (resolve P1) | ✅ DONE 2026-09-05 — commit `780bdb58` Ramzi_V2, CI green + post-deploy smoke, digest==image, browser-verified Chromium/WebKit/mobile/AR-RTL: save-draft button + resume banner restore work, publish UNREGRESSED, zero crashes. API needed NO change (draft already first-class: status defaults draft, drafts excluded from index/feed, moderation rejects draft→approved — source-verified). **P1 RESOLVED — `product-form-v2.tsx` now CLEAN.** UI added (save-draft button + resume banner) shipped — additive, publish unaffected. 9 open items (O1–O9) logged below. | S_CTO_11_END_SESSION_REPORT_2026_09_05.md |
| S-CTO-14 | EVENTS-vs-UI 360 (READ-ONLY map) | ✅ DONE 2026-09-05 (read-only, HEADs clean). All 492 events mapped: **157 LIVE (landing) + 128 WIRED-NOT-LANDING + 207 PAPER**. Key findings below. Its "S-CTO-15..18" follow-up numbering IGNORED (collides); Brain renumbers. | S_CTO_14_END_SESSION_REPORT_2026_09_05.md |
| S-CTO-12 | JOB/SERVICE WEB FORM (image-optional) | ✅ ACCEPTED (SELLER SIDE) 2026-09-05 — commit `7010ea6d` Ramzi_V2, deployed, browser-verified Chrome+WebKit+AR-RTL: job no-img ✓, service no-img ✓, physical blocked ✓, edit imageless ✓ (O-JS1 fixed), publish unregressed. **NOT DONE: buyer-render sweep** — photoless listing NOT verified on search cards / detail page / my-listings / Smart View / MCP cards / BO. If no image fallback, a photoless card may look broken (MEDIUM). → render-sweep owed (S-CTO-R-RENDER). Job/service = seller-complete, buyer-render UNVERIFIED. Minor: mobile physical-block unasserted (harness quirk), GHCR no git-SHA label (hardening), untracked test spec → commit as regression guard. | S_CTO_12_JOB_SERVICE_WEB_EVIDENCE_2026_09_05.md |
| S-CTO-15 | EVENT ALLOWLIST FIX + TRIAGE (api) — classify EVERY undeclared event A(has-UI→wire+declare)/B(no-UI→founder approve/reject)/C(junk→prune); ADD A; build reject-unknown that SURFACES (never drops/silent-accepts); fix stale doc; return FOUNDER B-LIST. | IN FLIGHT (authorized 2026-09-06, api writer) | S_CTO_15_ALLOWLIST_FIX_EVIDENCE_2026_09_05.md |
| S-CTO-15 | EVENT ALLOWLIST FIX + TRIAGE | ✅ ACCEPTED 2026-09-06 — commit `bd5cf2a` Ramzi_V2, CI green, live-verified. 28 events made canonical, reject-unknown validation built (surfaces unknowns, no drop — §50), stale doc fixed. Corrected S-CTO-13a/14 (some proposed events had no emitter — S-CTO-15 triage is the corrected truth). GAPS: Amplitude tracking-plan registration NOT done (I6 → S-CTO-18); web emitters still old names (S-CTO-16); only 1/28 runtime-exercised; DB row-parity via logs not psql (I7). B-LIST awaits founder ↓. New: I1 DEFAULT partition holds 8 events (dormant alarm) — routing probe owed. | S_CTO_15_END_SESSION_REPORT_2026_09_05.md |
| S-CTO-20 | MODERATION-360 (READ-ONLY) | ✅ ACCEPTED 2026-09-06 (read-only, HEADs unchanged, ZERO writes — verified). MAP: **moderation is 100% MANUAL** — a BO admin "Verify" → `approvePublication()` (`publication.service.ts:3705`) sets BOTH `isModerated=true`+`isVerified=true` + pushes index. No auto-approver/worker/threshold. Fresh listings (photo OR photoless) are publicly INVISIBLE until a human moderates (search filters `isVerified`, index-write gates `isModerated`). **F1 🔴 CONFIRMED by Brain at real file: `feed-generator.service.ts:479` `.innerJoinAndSelect('pub.images')` → photoless listings SILENTLY DROPPED from ALL feeds** (supply leak; §50 violation). Two flags gate two layers (index-write vs search-read) — coupled only because manual-approve sets both; auto-mod branch sets only isModerated (F3 latent hazard). Live DB read BLOCKED (in-VPC). | S_CTO_20_MODERATION_360_EVIDENCE_2026_09_06.md |
| S-CTO-21web | PHOTOLESS RENDER LOOSE-ENDS FINISH (web) | ⚠️ FINISHED — INCOMPLETE (honest PARTIAL) 2026-09-06 — ZERO source changed (the render code was already shipped by S-CTO-18web `8b0a815f`; digest unchanged, no drift). RE-PROVEN live: My Listings photoless→icon (4 configs, 0 404). DEFERRED (hard-dep on moderation): detail-page + buyer-card render — unmoderated photoless listings 404 publicly so can't be viewed. C0 meta: prompt asked to build already-shipped code (prompt-authoring signal — fold future render proof into the moderation-unblock). | S_CTO_21web_END_SESSION_REPORT_FOR_BRAIN_2026_09_06.md |
| S-CTO-22api | FEED PHOTOLESS-DROP FIX (F1) | ✅ ACCEPTED (fix shipped) 2026-09-06 — commit `8d55a20` Ramzi_V2, verified: `feed-generator.service.ts:485` now `.leftJoinAndSelect('pub.images')` (was inner), CI-green, deployed. Zero-photo listings no longer dropped. Guards preserved. **BUT F1 has TWO shapes — only shape-1 (literally zero image rows) fixed:** a photoless-shoes listing was already getting through because a video/cover counts as an image row. Real job/service listings' actual shape UNVERIFIED (DB IP-locked) → S-CTO-25api must seed + observe a real one. **NEW distribution findings below — some ban-adjacent, need attention.** | S_CTO_22api_END_SESSION_REPORT_FOR_BRAIN_2026_09_06.md |
| S-CTO-23qa | MODERATION→VISIBILITY RUNTIME QA (READ-ONLY + 1 human verify) | ✅ QA-ACCEPTED by B23 2026-09-06 (read-only, all HEADs clean, no commits). Report NOW on disk + source-verified by B23. **Chain PROVEN live:** published→unverified = invisible in public search; founder BO Verify (real 2FA = the human gate) flipped `isVerified=true`+`isModerated=true` → entered keyword index → searchable. B23 source-confirmed all 3 core claims: D1 anomaly REAL (`publication.controller.ts:626` sets isModerated=true at publish ONLY inside `if(enableModeration)` = `ENABLE_CONTENT_MODERATION==='true' && x-api-version==='v2'`, l.531 — so a false readback means that branch didn't fire; trace owed), D2 REAL/LOW (`hybrid-search.service.ts:443` `executeVectorSearch` bare `knn`, NO isVerified/status filter unlike keyword leg l.313 `[{status:published},{isVerified:true}]` — unverified embeddings kept out only by score floor, not a hard filter; hardening candidate), drift CONFIRMED (`verifyPublication:3295`→wraps→`approvePublication:3705`, atomic isModerated+isVerified+index). Session honestly WITHDREW an over-claimed "leak" after founder challenge (§52.2 respected). NOT verified (honest, VPC-locked): web browser render of pending-vs-verified (§47 gap), `bo_moderation_audit_log` row insert, approve WhatsApp/email side-effects, direct DB reads. **R0 CLEANUP OWED (HYGIENE):** synthetic staging data left live — esp. VERIFIED synthetic listing `bfa8710e` now IN staging search; also `03fceb84` (embeddings-only), draft `cf108cfc`/`22f2a052`, 4 synthetic sellers. Must be removed. | S_CTO_23qa_END_SESSION_REPORT_FOR_BRAIN_2026_09_06.md + S_CTO_23qa_MODERATION_VISIBILITY_EVIDENCE_2026_09_06.md |
| S-CTO-24 | FRONTEND REGRESSION 360 (READ-ONLY, web vs prod-main) — diagnose ALL 6 regressions (FR-1 pink rect, FR-2 auction card+hide/subscribe rule, FR-3 pagination, FR-4 chat, FR-5 desktop detail layout+vertical banner, FR-6 cards vs prod-main) from source: compare staging web vs prod-main, find each root cause + fix plan, grouped non-colliding. NO writes. Prints prod-main-vs-staging diff per component. | STAGED — fire the moment S-CTO-22api or S-CTO-23qa lands (hold to 2-session machine max) | S_CTO_24_FRONTEND_REGRESSION_360_EVIDENCE_2026_09_06.md |
| S-CTO-10 (READY, moderation-independent) | AUTH FLOW REDESIGN — login=password/forgot=OTP/signup=OTP + web research + end-to-end. RED, OTP-cost. Safe "other half" of a next pair anytime. | QUEUED (ready) | — |
| S-CTO-18web | PHOTOLESS JOB/SERVICE build + prove (web) | ⚠️ ACCEPTED PARTIAL 2026-09-06 — commit `8b0a815f` Ramzi_V2. PROVEN LIVE: My Listings shows photoless job/service w/ category icon (no 404, 4 configs); `feed_slide_change`→canonical `feed_slide_changed` in deployed bundle; photo listings unregressed. BUILT-NOT-VERIFIED (do NOT claim done): detail-page icon fix + buyer-card icon fix (never rendered live — photoless 404 publicly / not in search), emitter lake-landing (spelling only). ✅ Session self-caught its drift + STOPPED per the new ARCHITECTURE-MAP gate (gate working on 1st test). **REAL THREAD SURFACED = the moderation gate is the door into supply/demand (see M-ZONE below).** C1 feed-bug claim PARTLY REFUTED by Brain: eligibility uses `leftJoinAndSelect('pub.images')` (NOT inner) — photoless NOT dropped there; a specific Google-Merchant FORMATTER may still inner-join — UNCONFIRMED, needs api check. | S_CTO_18web_END_SESSION_REPORT_FOR_BRAIN_2026_09_06.md |
| S-CTO-19api | ADD CANONICAL EVENT `app_install_banner_dismissed` | ✅ ACCEPTED 2026-09-06 — added canonical, verified to LIVE DB (persisted row, no `_is_canonical` flag = canonical), digest==commit, CI green. Unblocks the web rename (S-CTO-20web). NOTE: entry has NO producer yet — web still emits old `app_banner_dismissed` (`OpenInAppBanner.tsx:231`) → lands as drift until the web rename ships. **NEW FINDING (E-BANNER, taxonomy-vs-reality):** allowlist has `app_install_banner_shown` + `app_install_banner_clicked` with ZERO web emitters; the banner actually fires `banner_view`/`banner_click`/`app_banner_dismissed` — a 3-name mismatch → founder/Brain naming decision (part of the naming reconciliation, NOT touched). Also: staging task-def has no container health check (cutover hardening); §28 parallel-worktree drift to watch. | S_CTO_19api_BRAIN_END_SESSION_REPORT_2026_09_06.md |
| S-CTO-16 | SEARCH-INDEX FLAG-A DIAGNOSIS (READ-ONLY, api) | ✅ ACCEPTED 2026-09-06 (read-only, api HEAD unchanged bd5cf2a, ZERO product writes — verified). VERDICT: search does NOT require a photo (source-proven) → FLAG-A is NOT a search-index bug. Likely real cause = front-end form doesn't submit photoless job/service OR stuck in moderation. **KEY TRAP (I-4): `service` is a CATEGORY not a TYPE — querying `type=service` silently returns 0** (probably explains the 0-result sighting). Staging OpenSearch holds only 39 listings, ZERO jobs/services at all. Gaps: DB read blocked, OpenSearch IP-locked (I-1 ECS-Exec agents not connected; I-3 staging serves media from prod-named S3 bucket — confirm). | S_CTO_16_END_SESSION_REPORT_FOR_BRAIN_2026_09_06.md |
| S-CTO-17web | CATEGORY-ICON FALLBACK + EMITTER RENAMES (web) | ⚠️ FINISHED — INCOMPLETE (BLOCKED) 2026-09-06 (research only, web HEAD unchanged 7010ea6d, ZERO product-src writes — verified; only untracked test/report artifacts). PROVEN from source: icon source = `getIcon(category.icon)` in `icones.ts` (never 404s, no new asset); the 404 is ONE spot = `ListingRow.tsx:35` (`placeholder.png`); buyer card uses valid `storecover.png`; detail media = empty box when photoless. BLOCKERS: **BLK-1** rename `app_banner_dismissed`→`app_install_banner_dismissed` blocked (canonical name NOT in API allowlist — needs an API session first). **BLK-2 → FOUNDER DECIDED 2026-09-06 = SKIP** `ai_generated` rename (it's a PROPERTY not an event; renaming corrupts payload). Left as-is. Closed. **BLK-1** (`app_install_banner_dismissed` allowlist add) → S-CTO-19api. → web BUILD = S-CTO-18web. | S_CTO_17web_EVIDENCE_2026_09_06.md |
| S-CTO-R-RENDER | PHOTOLESS-LISTING RENDER SWEEP (READ-ONLY) | ✅ ACCEPTED 2026-09-06 (read-only, HEAD `7010ea6d`). Verdict: BUYERS see photoless listings fine; SELLER "Mes ventes" shows a BROKEN thumbnail (fallback points at `placeholder.png` that 404s on staging — 1-line fix). NOT verified: Smart View card, MCP card, BO listing view (separate contracts, unreached). **FLAG-A (RED — bigger than the icon): published photoless services may be MISSING from the public search index** — `/publications/search` returned 0 photoless while seller published photoless services. Single unverified observation → needs an api/search diagnosis. FLAG-E: photoless detail page collapses the media column (optional polish). Created untracked test artifacts under tests/e2e-staging/scto-r-render/ (no product code, uncommitted) — flagged for cleanup. | S_CTO_R_RENDER_END_SESSION_REPORT_2026_09_06.md |
| S-CTO-13a | EVENT NAMING RECONCILIATION (READ-ONLY) | ✅ DONE 2026-09-05 (read-only, HEADs clean). Decision table produced. ROOT CAUSE of drift: the "reject unknown events → HTTP 400" contract was NEVER BUILT — ingestion persists unknowns as `_is_canonical=false`, so any mistyped name silently lands. Smart View: ~24 emitters, only 7 declared → ~17 undeclared + ~7 wired-unproven + 1 declared-not-wired. Draft: 4 undeclared + all 5 unregistered on Amplitude plan. **BIG FIND — MCP is a separate 11-event island → local SQLite /tmp, NO bridge to the sovereign lake; the 4 canonical `mcp_*` events never emit** (Commandment-2 violation: ChatGPT commerce NOT feeding the lake). 14 open items (O1–O14) + 2 founder micro-decisions below. Its "S-CTO-15/15b/16/17" numbering IGNORED (collides); Brain renumbers. | S_CTO_13a_END_SESSION_REPORT_2026_09_05.md |

---

# ═══════ LIVE CTO STATE — 2026-09-05 ═══════

### 1. CURRENT STATE (✅VERIFIED this session)
- Repos all on `Ramzi_V2`, nothing in flight: web `36b7183c` · api `bceef76` · bo `07b3a84` · mcp `d8efb4a`. Only untracked dirt (yarn.lock, report dirs) — no committed work pending.
- **App stream exists (newly inventoried):** `tawadoo_mobile_app` (backend-wired, branch `Ramzi_V2`, last commit **Jul-19**, ~204 src files) + `tawadoo_app_mobile_ui_only` (branch `new_design`, last commit **Jul-20**, ~721 files, UI redesign). **Both ~7 weeks behind api/web** — a launch-relevant staleness gap, UNVERIFIED against current API contracts.
- **ChatGPT/MCP stream exists:** `-tawadoo-mcp-`, ~33 tool-def references, LIVE at `mcp.tawadoo.ma`. V1→V2 capability/contract inventory NOT yet done.
- Prod is LIVE + healthy (📄 Aug-29): ~57,324 users / ~21,434 published, running the LEGACY stack. New analytics/intelligence platform lives ONLY on staging Ramzi_V2, activates at cutover.

### 2. COMPLETED + VERIFIED (frozen — do not reopen)
Commerce O1 chain create→pay→publish→approve→searchable LIVE · BO coin-ops super-admin+audited (live) · schema-ownership S141 · AI-listing FIX-001..015 (code) · sovereignty lake DB-first+idempotent · CORPUS-JOIN P1/P1B/P2 · BO RBAC 80-item matrix / ~67 resources (source) · Payzone signature verify · MCP security closure · private-message no-store+CSP.

### 3. RED — RELEASE BLOCKERS (must fix before launch)
- **R-1 CLOSE-U3 (identity/delivery):** money events (offer/bid/coin/payment/order/lead) land the lake; whether they reach Amplitude under the RIGHT identity is a **doc-vs-source CONFLICT** (audit says no; source shows sovereign emit + worker delivers on real user_id). Data-integrity/attribution risk. **Must be settled by a live read before any fix.**
- **R-2 customer-facing 500 — NAME CORRECTED + STATUS DOWNGRADED 2026-09-06:** the route `/publications/my-publications` DOES NOT EXIST (source-verified). The real seller-listings endpoint is **`GET publications/mine`** (`publication.controller.ts:206`); the user-facing screen is **"My Listings" / "Mes ventes"** (`myAnnouncements/my-announcements-view.tsx`). The "500 on my-publications" was a 📄DOC claim I carried WITHOUT source proof — treat as UNVERIFIED, NOT a confirmed blocker. → needs a live read-only check of `publications/mine` before it's called RED. (Correction: Brain previously hallucinated the route name from a stale doc.)
- **R-3 §28.5 security drift:** 2 prod security fixes (`6e06278` JwtAuthGuard on message/report; `1c714d6` Cache-Control no-store) MISSING from Ramzi_V2 → a live V2 auth/privacy gap. Founder merge decision.
- **R-4 media same-item guard is client-side only** (🔴 B1) — server/API/mobile/agent bypass. Integrity of listing media.
- **R-5 cohort cron crashes every run** (📄) — must be fixed before the platform activates at cutover.
- **R-6 App staleness:** app repos ~7 weeks behind API — UNVERIFIED whether current auth/contracts still work. Launch stream.
- **R-7 ChatGPT/MCP V1→V2 contract preservation:** no inventory yet; breaking a public MCP contract at cutover is a launch risk.

### 4. YELLOW (high-leverage, fix if small)
Event attribution threading (unblocks funnels) · ~260 paper-only events → wire the ones with a real surface / prune dead · search-intelligence BO read-surface (image/category/language/location) · Smart View runtime quality (founder says weak) · Node-20 CI deprecation · API admin tests not in CI.

### 5. BLUE (post-launch, do NOT hijack release)
Bayesian/HMM future model · full ACP buyer-transact + seller-draft · RTB/advertiser agent + campaigns · price-suggestion V2 · design-system/motion consolidation · broad §28 drift · GA4 MCP (Google auth) · sGTM ownership · unified behavioral-intelligence asset.

### 6. ARCHITECTURE (R-state)
R1 (touch-now when we enter CLOSE-U3): analytics identity contract — one authoritative user vs entity id. R2 (deferred debt): two parallel truth stores (`ta_activity_history` vs `ta_analytics_event`); provider adapters/Plugs consolidation. R0: none open beyond R-1's identity risk. R3: cosmetic — none active.

### 7. VERIFICATION GAPS (exists, not proven)
Money-event Amplitude receipt (R-1) · app runtime vs current API · MCP V2 tool behavior · Smart View real runtime · Tawssil never carried a real parcel · RTB serves nothing (zero campaigns) · search hybrid/vector live-serving.

### 8. APP — sync state
UNVERIFIED. Two repos, ~7 weeks stale, one backend-wired (Ramzi_V2) + one UI-only (new_design). No proof current auth/listing/buyer flows still work against api `bceef76`. Owed: a real app audit (§15).

### 9. CHATGPT/MCP — migration state
LIVE V1 at mcp.tawadoo.ma, ~33 tools. V1 capability + public-contract inventory NOT done. Owed: inventory → map to V2 → preserve contracts → staging test → fallback → cutover (§14).

### 10. DEPLOYMENT / CUTOVER
Staging is the proving ground; prod protected. Cutover checklist (10 items, A11 of the orchestration brief): §28.5 merge, secret rotation, app sync, GMC appeal, DMARC, CNDP, Aurora Multi-AZ, 20K reindex, GA4 receipt, analytics platform migration-path + cohort-fix + allowlist-prune.

### 11. RISKS (real, current)
Doc-vs-source conflict on money delivery (could waste a build if we "fix" the wrong layer) · app 7-week drift could hide breakage · MCP contract break at cutover · machine crashes if >2 concurrent sessions.

### 12. NEXT SESSION (staged, NOT fired — founder GO'd the unit, prompt ready; I am driving)
**PRIORITY UNIT (founder-elevated 2026-09-06) — classification A refactor/architecture — AHEAD of the FR wave.** Founder identified a SYSTEMIC entity-addressing/navigation-integrity defect (the "item no longer available", broken share, dead deeplinks, notif-can't-find-target class). Two-stage program:
- **S-CTO-25-NAV-MAP (Stage 1, READ-ONLY, runs ALONE):** determine whether Tawadoo V2 has a systemic entity-addressing/navigation defect and, if so, establish the canonical contract. Trace 10 representative entities end-to-end across web+api+search/index+BO+DB; app+MCP = contract inventory only (no fix). Returns 7 artifacts: Addressing Matrix · Failure Matrix (every "unavailable"→STATE 1–12) · Root-Cause Patterns · Canonical Target Model · Migration Surface · App/MCP Compat Notes · Stage-2 Architectural Decision. **Founder constraint: derive canonical identity from the SYSTEM OF RECORD (DB→Core→API→projection→URL→notif→share→app→MCP), NOT from the web impl.** DO NOT IMPLEMENT.
- **S-CTO-26-NAV-FIX (Stage 2, BUILD):** implement the smallest correct canonical mechanism + migrate callers + regression matrix (search→detail, share→fresh-browser, notif→target). Fired ONLY after founder approves the Stage-1 map + target model. Then App+MCP conform to the contract in their own units.

### 13. WHY NEXT (highest leverage)
Founder-elevated over the FR wave: the FRs (FR-9 notif deadlink, FR-11 mark-lead, "item unavailable", broken share) are SYMPTOMS of one architectural class, not 11 isolated bugs. PATTERN-OVER-INSTANCE: fixing the entity-addressing contract at the root makes the product progressively harder to break, vs patch-on-patch. This IS the refactor. Stage 1 is read-only = machine-safe, zero regression risk, and it makes the Stage-2 implementation obvious. FR wave resumes after (those are genuine frontend-only issues that do NOT share the addressing root cause).

### 14. SESSION PROMPT
→ written + ready: `KIRO_EXEC_PROMPT_S_CTO_25_NAV_MAP_2026_09_06.md` (10/10, read-only, Scope A, 7 artifacts, source-of-record-first). Awaiting founder GO to fire (runs alone).

### 14b. PARKED — FR WAVE (resumes after NAV-FIX lands)
Prompts already written, source-verified, NOT fired: `KIRO_EXEC_PROMPT_S_CTO_25_FUNCTIONAL_FRONTEND_2026_09_06.md` (FR-3 pagination + FR-11 mark-lead + FR-8 badge/mark-all-read + FR-9 notif deeplink) + `KIRO_EXEC_PROMPT_S_CTO_26_REMOVE_PINK_SAVESEARCH_2026_09_06.md` (FR-1 pink + FR-7 save-search). ⚠️ NOTE: FR-9 (notif deeplink) + FR-11 (mark-lead) + the "item unavailable" pattern OVERLAP the NAV-integrity unit — after NAV-FIX, RE-SCOPE these two FR prompts so they don't duplicate/collide with the canonical mechanism (they may become thin conform-to-contract fixes). FR-1/FR-3/FR-7/FR-8 are addressing-independent and stay as-is. Renumber the parked functional/pink prompts (their S-CTO-25/26 numbers are now taken by NAV) when they resume. Behind them: S-CTO chat (FR-4) · visual (FR-5 layout + FR-6 cards) · FR-2 auction subscribe-notify · search-intelligence. D1/D2 fold into moderation/api touch.

---

### PARKED-ITEMS REGISTER (nothing lost — from landed sessions)
**From S-CTO-3 (money identity fix), 11 open items:**
- OPEN-U3-1 (YELLOW): store-boost web UI not browser-verified (API proven, not Chromium/WebKit). → fold into a web QA pass.
- OPEN-U3-2 (YELLOW/R2): historical mis-keyed money events NOT backfilled (forward-fix only). → separate backfill unit, post-launch-safe.
- OPEN-U3-3 (BLUE): tagged staging test data left in place. → cutover cleanup.
- OPEN-U3-4 (YELLOW): ECS Exec channel down on back service (pre-existing) — blocks in-VPC DB reads. → infra fix, needed for future live DB verification.
- OPEN-U3-5 (YELLOW): Amplitude `order_id` may still be a USER-scoped property from the old bug. → Amplitude taxonomy cleanup (property re-scope).
- **OPEN-U3-6 (RED — NEW, surfaced to founder): `POST /auth/register` HANGS on staging** — email-hygiene MX DNS lookup times out because the VPC blocks outbound MX resolution. Blocks signup via the normal path on staging; prod parity UNVERIFIED. → this is a launch blocker if it also affects prod; must verify prod + fix.
- OPEN-U3-7 (YELLOW): 6 failing test suites on clean HEAD (entity-boot, hybrid-search ×2, whatsapp ×3) — pre-existing, makes CI non-deterministic. → test-health unit.
- OPEN-U3-8 (BLUE): dirty lint baseline. → hygiene.
- OPEN-U3-9 (YELLOW): very noisy SQL query logging on staging back — cost/observability. → quiet it (cutover-adjacent).
- OPEN-U3-10 (parked/B): `whatsapp_sent` identity not changed (messaging, deliberate). → messaging identity unit later.
- OPEN-U3-11 (RED/moat, = OPEN-7): `prediction_conversion_scored` rejected on system-uuid "system" → lake data loss. → identity-contract fix (same class as the money fix, for system events).

**Note:** R-1 (money attribution) is now CLOSED. OPEN-U3-6 (register hangs) is a NEW RED added to the blocker list. OPEN-U3-11 (system-uuid reject) remains the last identity-contract gap.

**From S-CTO-5 (AI-listing journey/video), open items — NOTHING buried:**
- **O1 (RED / R1) — FOUNDER DECIDED 2026-09-05 = YES (option A):** jobs & services = image-OPTIONAL; physical goods (car, electronics, etc.) still image-REQUIRED. Server-side rule (api). → build session S-CTO-8. Also fix the web side (P4/O-web: job/service not exempt from the image rule + in-form record button O4) once `product-form-v2.tsx` uncommitted work (P1) is resolved.
- O2 (YELLOW): could NOT positively confirm a video renders on a PUBLIC listing — fresh listings 404 publicly because `isModerated=false` (moderation is a separate step; car listing 404'd the same way). Need to confirm on an already-moderated listing. Not a video bug.
- O3 (YELLOW): moderation pipeline not mapped (partly in BO) — Brain to map how a listing goes moderated=true.
- O4 (YELLOW): in-form Videos tab has NO record button (record only exists on the wizard step) — UX inconsistency.
- O5 (YELLOW): exact coin debit on publish not verified.
- O6 (YELLOW): not verified the OpenSearch doc actually carries the video after push.
- O7 (YELLOW): analytics/lake — NOT verified create/video events actually land.
- O8 (BLUE): 11 WebKit console errors on the create page.
- O9 (BLUE): aggressive digit-run content-policy rejection; SSR/client lightssr split note.
- **P1 (blocker for clean web edits):** the uncommitted S-CTO-2 draft/autosave work is SITTING IN `product-form-v2.tsx` — blocks a clean edit of that file until resolved (commit via the parked S-CTO-4 finisher, or stash). MUST resolve before the next web write to that file.
- P2 (BLUE): dirty yarn.lock. P3 (BLUE): untracked QA scaffolding.
- P4 (YELLOW): FE/server media-rule disagreement (same root as O1).
- **P5 (security — GOVERNANCE) — FOUNDER DECIDED 2026-09-05 = DEFER to CUTOVER/pre-prod (not now):** Google Maps API key value in `.env.staging`. Action at cutover: verify if committed, restrict/scope the key, rotate. Added to the go-live security checklist. Do NOT action now.
- Server contracts learned (record, not bury): description 150-char minimum; digit-run content rejection. Two mis-leads DISPROVEN: "no video preview" = test-harness artifact (not a codec bug); "no public video" = the moderation gate (not a render bug).

**From S-CTO-7 (auth 3-methods diagnosis) — NOTHING buried:**
- **A-ESC1 (RED / security — PRODUCTION):** prod LEAKS an internal error message on a 500 (SSO google) that staging correctly hides → prod may be running DEV_MODE on or an old build. Info-disclosure on PROD. → verify prod build/DEV_MODE + fix. HIGH.
- **A-ESC2 (RED / security):** the rate limiter is BYPASSABLE by faking a header — proven live. Per-IP OTP protection is defeatable. → fix rate-limit to not trust a client-set header. HIGH.
- A-4known: disposable blocklist DEAD (staging+prod); prod email-hygiene WEAKER than staging; SSO 500s; the 409 "already exists" message getting flattened. (YELLOW cluster.)
- A-FE-GAP (RED-adjacent): NO frontend auth screen was browser-verified — register/phone-login/SSO UI unproven. → needs a web browser QA pass (after P1 draft cleanup, since auth screens may be safe but confirm repo).
- A-DB-GAP (YELLOW): DB writes (account row, phoneVerified) never directly confirmed — inferred from API only (ECS Exec down, OPEN-U3-4).
- **A-DEC1 — FOUNDER DECIDED 2026-09-05 = LEAVE the prod test accounts** (may reuse). No prod cleanup. Closed.
- **A-DEC2 — CORRECTED 2026-09-05 (source-verified): NOT a user-facing issue.** "SMS" appears ZERO times in web locale files (fr/ar/en.json all = 0). It exists ONLY in backend code: route names (`resend-code-sms`, `verify-code-sms`), API doc summaries, and a few server response `message` strings ("Le code OTP a été envoyé par SMS."). The frontend shows its own (correct) copy — users never see "SMS." So this is BLUE code-hygiene (rename internal labels when touching that file post-launch), NOT a §30 copy fix. No founder wording approval needed. Brain earlier OVER-STATED this as user-facing — corrected.
- SMS-mislabel confirmed (route `resend-code-sms`/`verify-code-sms` + "envoyé par SMS" copy actually WhatsApp) — BLUE code-hygiene (corrected: not user-facing).
- **A-FLOW1 (RED / cost + UX + security — FOUNDER RULE 2026-09-05, CORRECTED):** on email/phone entry, use `check-identifier` to BRANCH:
  · LOGIN (existing + remembers password) → PASSWORD, **NO OTP** (this is the cost saving — no OTP for normal returning logins).
  · FORGOT PASSWORD → **OTP IS REQUIRED** (it's the only proof-of-ownership, especially for WhatsApp-only users with no email — you CANNOT skip it; that's the point).
  · NEW SIGNUP → OTP as today.
  So OTP spend drops for the common case (normal login) but stays exactly where identity must be proven (reset + signup). Founder's own correction: reset must stay OTP.
  **MANDATORY WEB RESEARCH (§6/§52.2):** the session must web-search CURRENT best practices for OUR exact case — WhatsApp-OTP-only + no SMS + optional email + Morocco — (identifier-first auth, account-enumeration-safe responses, reset-token vs OTP, rate/cost controls) from authoritative sources, cited. Do NOT guess.
  **END-TO-END (do not forget):** cover events (login_password vs otp_sent vs reset flows → the lake), training-data implications, DB (identity/verified writes), and BO reporting on auth (login/signup/reset funnels). Verify the current flow from source FIRST (does the UI already call check-identifier and branch, or fire OTP blindly?) before building.

**From S-CTO-8 (job/service server) — open items:**
- O-JS1 (YELLOW): edit path of an imageless job/service will FAIL (same image rule, left untouched by scope). → fold into S-CTO-12 web or a small api follow-up.
- O-JS2 (YELLOW, 1-line): moderator approving an imageless job/service throws in the image-search-refresh background step (caught/logged, listing still approves). → quick api fix.
- O-JS3 (YELLOW): moderation-OFF publish path untested (staging runs moderation on). → investigation.
- O-JS4 (YELLOW): publication tests not in CI (ran locally only; widening touches frozen CI files). → test-in-CI unit (careful, frozen files).
- O-JS5 (NOTE): staging DB secret named "prod"-flavored (`dbname=twdbprod`, task role `tw-prod-…`) though genuinely staging — footgun; add clarity note before any DB work.

**From S-CTO-9 (auth security) — open items + DECISION:**
- **A-ESC1 → FOUNDER DECIDED 2026-09-05 = HOLD (do NOT touch prod now).** Prod leak-fix rides along automatically at cutover (Ramzi_V2 → prod in a few days). Staging already correct. No one-off prod deploy. Closed until cutover.
- O-SEC1 (YELLOW): trust-proxy is GLOBAL — changes `req.ip` for ALL consumers (analytics/ad IP-hashing, geo-country). Likely improvement, but verify nothing downstream assumed the ALB IP.
- O-SEC2 (RED-adjacent): BO + MCP sit behind the same ALB and may share the spoofable rate-limit pattern — read-only audit owed (extends the fix to those repos).
- O-SEC3 (YELLOW): `send-email-otp` throttler only partially proven (its 400 masks the 429) — boundary-test later.
- O-SEC4 (BLUE): 1 synthetic staging user left by S-CTO-9 (harmless) — cleanup at convenience.
- SERIALIZATION NOTE: S-CTO-8 + S-CTO-9 both committed to api Ramzi_V2 close together (11:37) — no conflict, clean ancestor, both live-confirmed. Two api writers at once worked this time but stay wary (prefer 1 writer/repo).

**From S-CTO-14 (events-vs-UI 360) — THE EVENT MAP, nothing buried:**
- **THE NUMBERS (source-verified):** 492 events total = **157 LIVE (landing)** + **128 WIRED-NOT-LANDING** (code emits, but no real flow triggered it in staging) + **207 PAPER** (declared, no emitter).
- **E1 — MONEY FUNNEL wired but never landed:** offers/bids/orders/payments/coins/rewarded-video/subscriptions emit in API+web code but NEVER landed the lake — because no real buyer completed those flows in staging. NOT a rebuild. → needs ONE real end-to-end staging transaction to confirm (browser). This is the highest-value proof.
- **E2 — BO analytics gap:** 22 admin pages, only `ask_ramzi_interaction` fires. If admin actions must feed the lake (Commandment 2), that's a big gap. → BO emitter wiring unit.
- **E3 — DARK FRONT SURFACES (low-risk build wins):** listing-card clicks / phone-reveal / video-progress; half of every chat (read/received/media); offer-modal funnel steps; WhatsApp delivery callbacks. Real user actions, no emitter. → emitter-wiring unit (user-facing, founder rule).
- **E4 — FOUNDER DECISION (naming drift):** 40 event names LAND that aren't in the allowlist (incl. 17 undeclared `smart_view_*`). Rename / add / prune? Recommend doing this FIRST before more wiring. → NAMING RECONCILIATION.
- **E5 — stale-doc lie:** allowlist comment says unknown events → HTTP 400 rejected, but code actually PERSISTS them with `_is_canonical=false`. Correct the comment.
- **E6 — MCP separate event list:** `-tawadoo-mcp-/server.py` has its own event list not matching canonical taxonomy — second source of drift.
- **E7 — historical incident (already fixed, record so not reintroduced):** old `'system'` string→UUID rejection dropped ~1,590 events/7d; fixed in current Ramzi_V2 via null user_id (= OPEN-U3-11 class, now understood as resolved for this path).
- **E8 (verification gaps):** DB↔Amplitude↔S3 parity NOT verified (landing proof from Amplitude only — is there also a `ta_analytics_event` row + S3 training pair per event?); landing is binary (seen/not-seen, not volume).
- **KEY REFRAME:** the ~207 "paper" + 128 "wired-not-landing" are NOT mostly "build new UI" — a huge chunk is **existing flows that simply were never exercised in staging** (money funnel) or **existing buttons missing a fire-event** (dark front surfaces). So the fix is mostly WIRE + PROVE, not build-from-scratch. Founder was right: less work than it sounds.

**From S-CTO-11 (draft finisher) — open items:**
- O1 (YELLOW): save-as-draft full round-trip (Save → appears in Mes ventes/Brouillon → open → publish) NOT clicked live w/ real account+coins; button+autosave/resume proven, round-trip source-confirmed not runtime. → fold into a create-journey QA.
- **O2 (YELLOW — the one real housekeeping):** register the 5 draft events on the lake tracking plan (they fire but aren't in the taxonomy). → part of the naming/taxonomy reconciliation (E4).
- O3 repeated-save dedupe not live-tested · O4 coin behaviour on save-draft unverified · O5 autosave field coverage (no media/auction-dates by design) · O6 BO render of drafts · O7 photo same-item "unknown" policy (FOUNDER call) · O8 legacy-form retirement (founder-gated) · O9 event-name mismatch (= E4).
- CI-NOTE: CI does NOT run the unit suite (176/176 + build were LOCAL evidence, not CI-certified) — systemic gap, recurring across sessions. Node-20 deprecation warning in CI.
- PROMPT-QUALITY NOTE: the dirty tree had layered dirt from S-CTO-2 + S-CTO-5; prompt premise slightly off — recorded for prompt authoring.

**From S-CTO-13a (naming reconciliation) — decision table + 14 open items:**
- **N-ROOT (mechanism of all drift):** ingestion NEVER built the "reject unknown → 400" contract; unknowns persist as `_is_canonical=false`. → the ADD/PRUNE build must also fix this (validate against allowlist) + correct the stale doc comment.
- **N-SMARTVIEW:** ~17 undeclared `smart_view_*` events landing; ~7 wired-unproven; 1 declared-not-wired (`smart_view_no_results_recovery`, O5). → ADD the legit ones to the allowlist (they matter for the Smart View completion work).
- **N-DRAFT:** 4 draft events undeclared + all 5 unregistered on Amplitude plan (= O2). → ADD.
- **N-MCP (RED / sovereignty — Commandment 2):** MCP = separate 11-event island → local SQLite in /tmp, NO lake bridge; the 4 canonical `mcp_*` events never emit. ChatGPT commerce is NOT feeding the sovereign lake/training data. → MCP→lake bridge = its own founder-gated session (touches the published MCP contract — careful).
- **O4 (collision):** `search_refined` emitted canonically on web (`useSearchTracking.ts:111`) AND as an MCP behavior event — same string, two taxonomies. → disambiguate.
- **N-DEC1 — CTO-DECIDED 2026-09-05 (founder said "move"):** `context_switch` → ADD as a Classic↔Smart view-switch event (behavioral signal for Smart View work). If S-CTO-15 finds it's actually role-related, flag + rename to `role_switched` instead. Founder can override.
- **N-DEC2 — CTO-DECIDED 2026-09-05:** `ai_generated` → `ai_generation_completed` (matches the `*_completed` convention); `price_suggestion_shown` → KEEP as-is (already clear/canonical). S-CTO-15 confirms against taxonomy; founder can override.
- Remaining O-items (O1–O3, O6–O7, O10–O14): allowlist ADD/PRUNE specifics, doc fix, DB↔Amplitude↔S3 parity unverified, BO emission gap (inherited from S-CTO-14), all catalogued in the report.
- RECONCILED PLAN (Brain-renumbered): **S-CTO-15 = allowlist ADD/PRUNE + build the reject-unknown validation + doc fix** · **S-CTO-16 = emitter renames (web)** · **S-CTO-17 = MCP→lake bridge (founder-gated, external contract)** · **S-CTO-18 = DB parity + BO emission gap**.
